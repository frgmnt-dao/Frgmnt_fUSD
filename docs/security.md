# Security Model

## Overview

Frgmnt fUSD is built with a layered security model: access control limits who can perform privileged operations, the guard system limits what the vault can do, oracle safety mechanisms protect against price manipulation, and a governance timelock prevents unilateral admin actions.

---

## Roles and Permissions

### Role Matrix

| Role | Contract | Key Permissions |
|------|----------|----------------|
| **Owner** | PoolLogic | `renounceOwnership`/`transferOwnership`; one-time `initializeAutoCompounding()` |
| **Manager** | Managed (via PoolManagerLogic) | Configure vault assets, set fees, set withdrawal mode, change manager; `setImmediateWithdrawEnabled`, `finalizeCashWithdraw`, `execTransaction` on PoolLogic |
| **Trader** | Managed (via PoolManagerLogic) | Execute guarded vault transactions via `execTransaction` (cannot toggle withdrawal mode or finalize requests) |
| `DEFAULT_ADMIN_ROLE` | TokenLogic | `grantRole`/`revokeRole`, `upgradeToAndCall`, configure assets, cooldown, deposit caps/minimums, `setPoolLogic`/`setPoolManagerLogic` |
| **poolLogic module** (implicit) | TokenLogic | Exclusive access to uncapped `mintFromPool()`; replaceable at any time by `DEFAULT_ADMIN_ROLE` |
| `EMERGENCY_ROLE` | TokenLogic | Pause / unpause deposits and `mintFromPool()` |
| **Owner** | Governance | `setContractGuard`, `setAssetGuard` — the guard registry every vault transaction is checked against |
| **factoryOwner** | PoolManagerLogic | Set fee caps, register pools, `setGovernance`/`setAssetHandler` (can swap either reference to a new instance wholesale), `setFactoryOwner` |
| **Owner** | AssetHandler | `setChainlinkTimeout`, `setSequencerUptimeFeed`, `addAsset`/`addAssets`/`removeAsset` — price-feed configuration for every priced asset |
| **Owner** | AaveV4SpokeManager, AaveV4TokenizationManager, MorphoVaultV2Manager | `setPoolReserves`/`setPoolVaults` — the per-pool allowlist each guard consults for onboarding new reserves/vaults |
| **Timelock (DAO)** | Governance, AssetHandler, PoolManagerLogic.factoryOwner (incl. PoolManagerLogic's UUPS upgrade authority, since `_authorizeUpgrade()` there is `onlyFactoryOwner`), PoolLogic.owner, TokenLogic.DEFAULT_ADMIN_ROLE (incl. TokenLogic's own UUPS upgrade authority) — once `scripts/transferRoles_Governance.ts` is run (see below) | Guard/price-feed/factory-reference changes, PoolManagerLogic/TokenLogic upgrades, TokenLogic admin operations, and PoolLogic's `initializeAutoCompounding()`, once wired; proposed via multisig with a 48-hour delay |
| **Proposers / Executors** | Timelock | Propose and execute DAO operations |

### Key Modifiers

| Modifier | Source | Applied to |
|---------|--------|-----------|
| `onlyOwner` | Ownable (OZ) | Governance guard registration |
| `onlyManager` | Managed | Fee changes, asset management, pool config |
| `onlyManagerOrTrader` | Managed | Transaction execution |
| `onlyFactoryOwner` | PoolManagerLogic | Factory-level configuration |
| `onlyRole(DEFAULT_ADMIN_ROLE)` | AccessControl (OZ) | TokenLogic admin operations |
| `onlyRole(EMERGENCY_ROLE)` | AccessControl (OZ) | Pause/unpause |
| `whenNotPaused` | Pausable (OZ) | Deposits, minting |
| `nonReentrant` | ReentrancyGuard (OZ) | Deposit, stake, unstake, withdraw |

---

## Guard System

The guard system is the protocol's primary defense against unauthorized vault transactions.

### Contract Guards

Before the vault can call any external protocol contract, a `txGuard()` validation must pass:

- **AaveLendingPoolGuardV3** — restricts Aave operations to supported assets, own-account-only recipients, variable-rate-only borrowing, and post-transaction health factor ≥ 1.01
- **MorphoBlueContractGuard** — restricts Morpho operations, validates market parameters, enforces health factor ≥ 1.01
- **UniswapV3RouterGuard** — restricts swaps to supported assets, tracks balances for slippage accumulation, limits multicall to single swaps
- **MerklRewardClaimGuard** (FNA-19) — protocol-agnostic claim-only guard for Merkl-distributed incentive rewards, registered once against Merkl's Distributor address; covers every integrated protocol's Merkl campaigns (Morpho Blue, Aave V4 Spoke, and any future one) through the same standard `claim()` interface, since Merkl's Distributor carries no protocol identifier. Enforces the claim's `users[]` is exactly `[poolLogic]` — a pool can only claim its own rewards.

### Asset Guards

Asset guards are responsible for computing balances and generating safe withdrawal transactions:

- **ERC20Guard** — validates `approve()` targets have registered guards; prevents approvals to unguarded addresses
- **AaveLendingPoolAssetGuard** — manages flashloan-based unwinding of Aave positions with configurable slippage tolerance
- **MorphoBlueLendingPoolAssetGuard** — manages Morpho position unwinding; net balance = collateral + supply − debt
- **UniswapV3AssetGuard** — values LP positions using TWAP pricing (not spot) to prevent manipulation

### Post-Transaction Checks

Both Aave and Morpho contract guards implement `afterTxGuard()` hooks that validate health factor after every transaction. A health factor below 1.01 causes the transaction to revert.

---

## Oracle Security

### Chainlink Staleness Checks

Each asset in AssetHandler has a configurable `chainlinkTimeout`. `getUSDPrice()` reverts if the last Chainlink update is older than this threshold, preventing stale price usage.

### L2 Sequencer Uptime

On Base (an L2), the AssetHandler checks the Chainlink L2 sequencer uptime feed before trusting any price. A 3600-second (1-hour) grace period is enforced after sequencer restart to allow prices to stabilize.

**Live-mainnet note (FNA-50)**: `_checkSequencerUp()` returns silently when `sequencerUptimeFeed` is unset — `initialize()` never configures it, and `setSequencerUptimeFeed()` is `onlyOwner` with no other call site outside deploy/upgrade tooling. Confirmed via direct `eth_call` (2026-08-30) that the live AssetHandler proxy's `sequencerUptimeFeed()` is still the zero address, meaning this protection has been a silent no-op since deployment. `deploy_core_contracts.ts` now sets and verifies it as a deployment invariant (before ownership transfers to `GOVERNANCE_SAFE`, failing the whole script if it doesn't land) for every future deployment, and `upgrade_core_contracts.ts` now includes a `GOVERNANCE_SAFE`-signed transaction to set it on the already-live proxy — but that transaction has not been executed yet, so the statement above describes the intended design, not the currently-deployed state, until it runs. The per-asset `chainlinkTimeout` check in `_readAggregatorPrice()` independently rejects genuinely stale answers regardless of this gap, bounding the residual exposure to feeds with long heartbeats and to the first minutes after a sequencer recovery.

### TWAP for Uniswap V3

The UniswapV3AssetGuard uses a configurable TWAP window (default: configurable by admin) rather than spot prices for LP position valuation, protecting against single-block manipulation.

---

## Cooldown Protection

fUSD enforces a time-weighted cooldown on cash withdrawals. The cooldown timestamp is a weighted average of mint events, making it impossible to:
- Deposit collateral and immediately cash out for arbitrage
- Flash-deposit to inflate `totalFundValue()` and withdraw at inflated prices

Cooldown does not apply to `unstake()` (sfUSD → fUSD) since sfUSD itself cannot be transferred and has its own entry-fee disincentive.

---

## Upgrade Security

All core contracts (TokenLogic, PoolLogic, PoolManagerLogic, AssetHandler) use the UUPS proxy pattern. The `_authorizeUpgrade()` function is restricted to `DEFAULT_ADMIN_ROLE` (TokenLogic) or `onlyFactoryOwner` (PoolManagerLogic).

As of the current deploy tooling, this authority is held directly by `GOVERNANCE_SAFE` at deploy time — `Timelock` is deployed alongside the rest of the stack but is not the actual owner/admin of anything until a separate, explicit step runs. `scripts/transferRoles_Governance.ts` (added for FNA-01) transfers `Governance.owner`, `AssetHandler.owner`, `PoolManagerLogic.factoryOwner`, and `PoolLogic.owner` to the Timelock, and moves `TokenLogic.DEFAULT_ADMIN_ROLE` to it via grant-then-revoke, as a final deploy step run once initial guard/asset bootstrap setup is complete (see Centralization Risks below).

Once the Timelock is actually wired in for a given role, it enforces a minimum 48-hour delay between proposal and execution of any change gated by that role.

---

## Slippage Protection

### SlippageAccumulator

The `SlippageAccumulatorUser` tracks cumulative slippage across all vault transactions in a time window. If total slippage exceeds the configured threshold, further transactions revert.

### Per-Operation Slippage

Withdrawal guards (AaveLendingPoolAssetGuard, UniswapV3AssetGuard) apply per-operation minimum-amount checks derived from oracle prices and configurable basis-point tolerances.

---

## Flash Loan Safety

### Aave Flash Loans

`PoolLogic.executeOperation()` is the Aave flash loan callback. It:
- Verifies the caller is the Aave V3 pool (registered as an allowed callback sender)
- Executes only the pre-encoded operations that initiated the flash loan
- Repays the loan within the same transaction

### Morpho Flash Loans

`PoolLogic.onMorphoFlashLoan()` similarly validates the caller is Morpho's core contract and executes only the pre-encoded unwinding logic.

### Callback Allow-List

`getAllowedCallbackSenders()` in PoolManagerLogic maintains a per-pool allow-list of protocol addresses permitted to call back into the vault. All other callers are rejected by the fallback function.

---

## Trust Assumptions

| Component | Trust Assumption |
|-----------|-----------------|
| Chainlink oracles | Price feeds are accurate and liveness-checked |
| Aave V3 | Protocol is non-malicious; aToken accounting is correct |
| Morpho Blue | Protocol is non-malicious; position accounting is correct |
| Morpho Vault V2 | Vault accounting (`convertToAssets`) and the curator's chosen adapters are non-malicious; `forceDeallocate` penalties are bounded on-chain by Morpho's own protocol-level cap, not by Frgmnt; mitigated by `MorphoVaultV2Manager` allowlisting only vetted vault instances per pool |
| Uniswap V3 | On-chain TWAP data is reliable over the configured window |
| Manager | Acts in good faith within protocol limits; cannot steal funds directly |
| Timelock + DAO multisig | Multisig signers are honest and keys are secure |
| Base sequencer | Sequencer is available; uptime feed is accurate (guard currently a no-op on the live proxy pending a `GOVERNANCE_SAFE` transaction — see the FNA-50 note under "L2 Sequencer Uptime" above) |
| Supported asset tokens (governance-whitelisted) | Behave as standard ERC-20: `transfer`/`transferFrom` move exactly the requested amount (no fee-on-transfer), balances don't change outside of transfers (no rebasing), and no external call/hook fires during a transfer to the pool or to the guard's underlying position (no ERC-777-style hooks) — see FNA-23 below for what's independently defended even if this assumption is violated |

---

## Centralization Risks (FNA-01)

The protocol has several privileged roles by necessity — active fund management, oracle/guard configuration, and emergency response all require *someone* to hold that authority. This section documents what each role can do, what currently backs it, and what's still open.

### Role authority summary

See the Role Matrix above for the full per-role permission list. In short: **PoolManagerLogic.factoryOwner** and **Governance.owner** are the highest-blast-radius roles — factoryOwner can repoint a pool at an entirely different Governance or AssetHandler instance, and Governance.owner controls the guard registry every vault transaction is checked against (`setContractGuard`/`setAssetGuard`). **AssetHandler.owner** controls price-feed configuration for every priced asset. **manager**/**trader** are lower blast-radius, constrained by the guard system itself (`PoolTxExecutor` requires an approved contract or asset guard for every `execTransaction` call) and cannot bypass it. **AaveV4SpokeManager/AaveV4TokenizationManager/MorphoVaultV2Manager owners** each control one integration's reserve/vault allowlist; a malicious entry still requires manager/trader to actually interact with it, and (per FNA-20/FNA-21) removing an allowlisted entry no longer blocks existing positions from being valued or exited.

### Withdrawal Centralization Risk (ERC20Guard.txGuard())

`ERC20Guard.txGuard()` permits an `approve()` call whenever the spender has *any* registered contract guard (`spenderGuard != address(0) && spenderGuard != address(this)`) — it does not separately verify the spender is a vetted protocol integration. This is accurate as described: it does not independently re-derive "is this a real protocol contract," it trusts the guard registry.

That registry is `Governance.contractGuards`, writable only via `Governance.setContractGuard()` (`onlyOwner`). So this isn't a distinct vulnerability requiring its own fix — it reduces to the same trust boundary as the rest of this section: whoever controls `Governance.owner` (and, transitively, `PoolManagerLogic.factoryOwner`, which can replace Governance entirely) can already authorize approvals to an arbitrary address by registering a guard for it. Mitigating the general Governance/factoryOwner centralization risk (via the Timelock wiring below) directly mitigates this specific attack path too, rather than needing a separate contract change.

### Current mitigations

- **Timelock exists and its lock-down tooling now covers every core-contract admin/owner role.** `scripts/transferRoles_Governance.ts` (added for FNA-01) transfers `Governance.owner`, `AssetHandler.owner`, `PoolManagerLogic.factoryOwner`, and `PoolLogic.owner` to the already-deployed `Timelock` (48-hour `minDelay`, `GOVERNANCE_SAFE` as sole proposer), and moves `TokenLogic.DEFAULT_ADMIN_ROLE` to it via a grant-then-revoke `AccessControl` sequence — all as one explicit final deploy step, run once initial bootstrap (asset/contract guard registration, `setup_Token_Logic.ts`) is complete. Before this fix, `Timelock.sol` was deployed but never actually assigned as owner/admin of anything, despite its own header comment stating that was the intent.
- **`AssetHandler`'s owner no longer defaults to the deployer key.** `AssetHandler.initialize()` runs `__Ownable_init(msg.sender)`; `deploy_core_contracts.ts` now transfers ownership to `GOVERNANCE_SAFE` immediately after deploy, matching every other core contract.
- **`GOVERNANCE_SAFE` is intended to be a Gnosis Safe multisig** (per `Timelock.sol`'s deployment comment), but **on the live USD deployment it is confirmed to actually be a plain EOA** (`0xafb9B883637f72767ADf7193Bb3B8e59C02Ea05d` has no contract code on-chain, checked directly via `eth_getCode` against Base mainnet). A single private key currently controls `Governance.owner`, `AssetHandler.owner`, and `PoolManagerLogic.factoryOwner`. `TokenLogic.DEFAULT_ADMIN_ROLE`, by contrast, is confirmed (via direct `hasRole()` calls — `false` for `GOVERNANCE_SAFE`, `true` for the address below) to already be held by a genuine 3-of-4 Gnosis Safe at a *different* address (`0x74aF72D91D5FB263fBa09Ed43aD1C1ea079058B3`, confirmed via `getOwners()`/`getThreshold()`), the same Safe that already holds `PoolLogic`'s `ProxyAdmin` and its own `onlyOwner` (via the pre-existing `scripts/transferRoles_poolLogic.ts`) — so today, two of the five core proxies/roles (`PoolLogic` and `TokenLogic`) are already multisig-protected, and three (`Governance`, `AssetHandler`, `PoolManagerLogic`) remain on the single EOA. Migrating those three to a real multisig (this DAO Safe or a new one) is a deliberate, separate decision for the team to make; not yet done.
- **Manager/trader authority is already constrained in code**, not just by trust: `PoolTxExecutor` requires an approved contract or asset guard for every `execTransaction` call, and Aave/Morpho positions are protected by `afterTxGuard()` health-factor checks regardless of which role initiated the transaction.

### Open items (deliberately deferred, not code gaps)

- **`AaveV4SpokeManager`, `AaveV4TokenizationManager`, `MorphoVaultV2Manager`** have no deploy script in this repo yet. This is deliberate, not an oversight: deploying them meaningfully also requires their companion contract/asset guards (`AaveV4SpokeContractGuard`, `AaveV4SpokeAssetGuard`, etc., each constructor-wired to its Manager), which in turn need real external protocol addresses (Aave V4 Spoke registry, Morpho Vault V2 instances) that don't exist as placeholders anywhere in this repo's deploy config — inventing them would be worse than leaving this explicit. Once that integration deployment happens, its ownership should follow the same two-stage pattern established here: `Ownable(msg.sender)` → transfer to `GOVERNANCE_SAFE` immediately after deploy (per the `AssetHandler` fix above) → transfer to `Timelock` as part of the final lock-down step (extend `transferRoles_Governance.ts` the same way `PoolLogic`/`TokenLogic` were added to it).
- **No DAO/voting module** — governance is multisig + timelock only, matching CertiK's "Short Term" recommendation tier, not yet "Long Term" (DAO) or "Permanent" (renounced ownership). Renouncing ownership is not applicable here: the protocol requires ongoing configuration (new asset/protocol integrations, oracle updates) that a fully immutable owner would prevent.
- **No public multisig-signer / timelock-address disclosure post yet** (the "medium/blog link" CertiK's template requests) — this is an operational/communications action outside this repository, not a code change.

---

## Known Risks & Mitigations

| Risk | Mitigation |
|------|-----------|
| Oracle price manipulation | Chainlink staleness checks; TWAP for Uniswap positions; sequencer uptime guard (pending activation on the live proxy — see FNA-50 note under "L2 Sequencer Uptime" above) |
| Flash loan attacks | Cooldown period on fUSD cash withdrawals; guard-gated execution |
| Manager fee extraction | Fee caps enforced by factory owner; increase delays |
| Unauthorized contract calls | Guard system; contract/asset guard registry |
| Reentrancy | `nonReentrant` on all state-changing user functions |
| Upgrade abuse | UUPS + Timelock; 48-hour delay on all admin actions |
| Health factor breach | `afterTxGuard()` post-transaction health factor validation |
| Unsupported debt during Aave/Morpho unwind | Guard enforces only supported assets can be borrowed |
| Collateral removal with open positions | `removeAssetCheck()` enforces zero balance before removal |
| `forceDeallocate` griefing of a pool's Morpho Vault V2 position | Permissionless on the vault itself — any third party can already call it against any pool's position independent of Frgmnt's guards; penalty is capped on-chain by Morpho's protocol-level maximum; `MorphoVaultV2Manager.getVaultAdapterPenalties()` lets governance review each adapter's configured penalty before whitelisting a vault |
| Single illiquid/misbehaving asset blocking fund-wide pro-rata withdrawals | `MorphoVaultV2AssetGuard.getBalance()` degrades to 0 instead of reverting on a misbehaving vault, so it can be removed via `changeAssets()` rather than bricking the pool; manager can switch to queued withdrawals (`setImmediateWithdrawEnabled(false)`) to decouple requests from requiring every asset to be liquid simultaneously |
| An under-liquid Aave V4 Spoke lending position (borrowers have drawn down the underlying) blocked the *entire* immediate pro-rata withdrawal, even for unrelated fully-liquid assets, whenever the calculated share exceeded available liquidity (FNA-07) | `FundCalculationLibrary.computeWithdrawableFundValue()` caps each `IWithdrawableBalanceGuard`-implementing position's contribution to NAV at its currently-available liquidity rather than its full claim, so one under-liquid position sizes its own share down instead of reverting the whole withdrawal. CertiK's follow-up flagged that this liquidity-capped figure was then also used as the *solvency* haircut's denominator (`applyClaimsHaircut`), converting a temporary, self-correcting liquidity shortfall into a permanent insolvency haircut on a fully-solvent pool. `computeImmediateWithdrawPortion()` now derives a separate, non-liquidity-capped (but still reserved-excluding) NAV for the solvency haircut, and only caps the resulting fair share against the liquidity-capped figure afterward. If that fair share itself still exceeds what's currently liquid, the withdrawal reverts (rather than silently under-delivering against fUSD already burned by the caller) — the user can retry with a smaller amount, wait for liquidity to return, or the manager can switch to queued withdrawals |
| `MorphoVaultV2AssetGuard`'s FNA-07 liquidity cap used `IERC4626(vault).maxRedeem(pool)` as a liquidity signal, but canonical Morpho Vault V2 implements `maxRedeem()` as a function that unconditionally returns 0 (it can't guarantee its dynamic gate/adapter simulation is revert-free from a view function) — every Morpho Vault V2 position therefore read as fully illiquid on every immediate withdrawal, unconditionally, silently excluding real, healthy positions from NAV available for immediate exit (FNA-25) | `MorphoVaultV2AssetGuard` no longer implements `IWithdrawableBalanceGuard` at all; `FundCalculationLibrary`'s fallback now treats it as fully liquid, matching this guard's pre-FNA-07 behavior — the immediate-withdrawal path sizes against the same full share balance `getBalance()` already reports. `withdrawProcessing()` no longer caps `sharesToRedeem` by `maxRedeem()` either. This reopens the underlying FNA-07 risk class specifically for Morpho Vault V2 (a genuinely under-liquid position's `redeem()` call can still revert its own withdrawal) — already an accepted, explicit scope decision documented in `withdrawProcessing()` (same risk class as an Aave/Morpho Blue market being fully utilized), and strictly better than the unconditional, silent exclusion this fix closes. The repository's `MockMorphoVaultV2` test double previously masked this by defaulting `maxRedeem()` to `balanceOf(owner)` instead of the real vault's always-zero behavior; it now always returns 0, matching production |
| CertiK FNA-07 follow-up: `AaveV4SpokeAssetGuard` capped each reserve's withdrawable amount independently against `IHubBase.getAssetLiquidity(assetId)`, so two reserves resolving to the same underlying `(hub, assetId)` pair (even across different Spoke instances, since one guard services any number of Spokes) could each see the Hub's full liquidity as available to them alone, letting their combined attempted withdrawal exceed what the Hub could actually deliver in one transaction | New per-call `HubLiquidityLedger` (in-memory, sized to `reserveIds.length`) threaded through both `getWithdrawableBalance()` and `withdrawProcessing()`: each reserve's liquidity claim is netted against what earlier reserves in the *same* call already claimed against the same `(hub, assetId)`, then records its own claim for subsequent reserves to see. Both call sites reconstruct the ledger independently but deterministically (same live state, same `reserveIds` order, no state mutation in between within one transaction), so NAV/portion sizing and execution can never disagree. Two new regression tests prove the combined withdrawable/withdrawn amount across two reserves sharing one Hub pool is bounded by that pool's real size, not double-counted |
| CertiK FNA-07 follow-up: `AaveV4TokenizationAssetGuard` was never covered by the original FNA-07 pass — it sized `redeem()` from the pool's full share balance regardless of the Hub's real available liquidity for the vault's underlying | `AaveV4TokenizationAssetGuard` now implements `IWithdrawableBalanceGuard`, capping the redeemable share amount via `ITokenizationSpoke(vault).hub()`/`assetId()` (confirmed against Aave V4's published source — plain public getters, no raw struct decoding needed, unlike `ISpoke.getReserve()`) and `IHubBase.getAssetLiquidity()`. Unlike Morpho Vault V2's now-reverted first attempt (see FNA-25 above), `IHubBase.getAssetLiquidity` is a real, non-reverting liquidity read with no known false-zero failure mode, so this cap does not carry that same risk. **Known residual gap, documented rather than fixed**: this cap is only aware of this guard's own vault — a pool separately holding an `AaveV4SpokeAssetGuard` reserve drawing from the *same* `(hub, assetId)` is not covered (the two guards are separate contracts with no shared state); closing that fully needs a pool-level, cross-guard liquidity ledger, deliberately out of scope here |
| CertiK FNA-07 follow-up: `MorphoVaultV2AssetGuard`'s FNA-25 removal (above) left every Morpho Vault V2 position with no liquidity cap at all, reopening the original FNA-07 risk (a genuinely under-liquid position's `redeem()` reverting the whole withdrawal) with no mitigation | `MorphoVaultV2AssetGuard` now re-implements `IWithdrawableBalanceGuard`, but using a different, verified-safe signal from the one FNA-25 removed: the vault's own **idle** balance (`IERC20(underlying).balanceOf(vault)`, converted to shares via `convertToShares`). Confirmed directly against Morpho's published source (github.com/morpho-org/vault-v2) that `redeem()`/`withdraw()` draws down idle balance first and unconditionally before ever touching an adapter (`if (assets > idleAssets && liquidityAdapter != address(0)) { deallocateInternal(...) }`), with no separate fee-escrow or virtual-idle layer on that same raw balance — so capping by idle balance is a *provable* floor (redemption within it can never need an adapter, so can never revert for that reason), not an estimate. Also confirmed both `convertToShares`/`convertToAssets` round down (`mulDivDown`) against the same denominator pair, so `convertToAssets(convertToShares(idleAssets)) <= idleAssets` always holds algebraically — the returned share figure never overestimates what idle balance covers. Beyond the idle floor, the pre-existing accepted risk (an under-liquid position's `redeem()` can still revert) remains unchanged. Three existing tests that predated this fix (written against the FNA-25 zero-cap state) were updated: two needed the mock vault's idle balance funded to keep exercising their original "full/uncapped" scenarios, and one ("accepted risk: ... reverts") was rewritten because the fix makes that exact scenario no longer revert — that inversion is the fix working as intended, not a broken test |
| CertiK FNA-07 follow-up: Aave V3 (`AaveLendingPoolAssetGuard`/`AaveV3LendingPoolAssetGuard`) and Morpho Blue (`MorphoBlueLendingPoolAssetGuard`) — both leveraged, debt-bearing positions unwound via a single flashloan — still sized every collateral/supply withdrawal and debt repayment from the full, uncapped position, deferred at the original FNA-07 pass specifically because these two needed real design work (see "How to apply" note that used to live in memory for this) | Both guards now compute a single, uniform `_maxSafePortion()` ceiling and apply it to *every* leg of the unwind — collateral withdrawal, supply withdrawal, and debt repayment/flashloan sizing alike — never an independent per-reserve cap like the no-debt integrations (Aave V4 Spoke/Tokenization, Morpho Vault V2) already fixed. This is a deliberate, load-bearing design choice, not extra caution: debt repayment and collateral/supply withdrawal are scaled by the *same* portion to keep the position's health factor unchanged across a partial exit, and the flashloan is repaid entirely by swapping back everything withdrawn — capping one leg below the requested portion while leaving debt repayment (and the flashloan sized to fund it) at the full portion could under-fund the flashloan's own repayment, a worse and harder-to-diagnose revert than the plain liquidity revert this fix exists to avoid. **Aave V3**: confirmed against Aave V3's real source (`AToken.burn()`: `IERC20(_underlyingAsset).safeTransfer(...)` from the aToken's own balance; `ValidationLogic.validateWithdraw()` itself only checks the caller's aToken balance, not reserve liquidity) that `IERC20(underlying).balanceOf(aToken)` is exactly what a reserve can pay out right now — `_maxSafePortion()` takes the minimum such ratio across every collateral reserve. **Morpho Blue**: confirmed against Morpho Blue's real source (`Morpho.sol`) that only the *supply* leg (`withdraw()`) is liquidity-constrained by other users' borrowing (`totalBorrowAssets <= totalSupplyAssets` enforced post-withdrawal) — `withdrawCollateral()` has no such check, gated only by the position's own health factor, since Morpho Blue collateral is isolated per-position, never pooled the way supply is. `_maxSafePortion()` there therefore only scans supply positions, but the resulting ceiling is still applied to collateral and debt too, for the flashloan-funding reason above. Both contracts needed a new per-file `optimizer: {runs: 1}, viaIR: true` override in `hardhat.config.ts` (matching `PoolLogic.sol`'s existing precedent) after this fix pushed each over the EIP-170 24,576-byte deployable-contract-size limit (Aave V3: 25,243 → 20,692 bytes; Morpho Blue: 24,936 → 20,378 bytes). **Known, explicitly out-of-scope gap**: neither fix accounts for DEX-side swap slippage/depth when settling the flashloan — a different risk class than lending-market liquidity, partially covered separately by FNA-16's TWAP work. Morpho Blue also still has no FNA-35-equivalent unwind-cost (flashloan premium) haircut on its `getBalance()` figure — `getWithdrawableBalance()` there scales the plain gross balance, not a net-of-cost one; adding that haircut is a separate, un-filed gap. Several pre-existing tests in both suites predated any liquidity signal in their mocks (aTokens/markets with zero funded underlying/available-liquidity by default) and needed updating to keep exercising their original uncapped scenarios, exactly the same pattern as the Morpho Vault V2 fix above — one Morpho Blue test additionally needed its expected values computed via Morpho's real virtual-shares formula (`VIRTUAL_SHARES=1e6`, `VIRTUAL_ASSETS=1`) rather than hand-picked round numbers, since the offset is not negligible at the small share counts test fixtures otherwise use. **Live deployment note**: both guards are already deployed on Base mainnet (non-upgradeable, constructor-configured contracts registered by numeric asset type in `AssetHandler`, not proxies) with real user positions through them — swapping to the fixed guard requires re-applying every piece of live operational configuration (Uniswap V3 fee tiers/paths, `requiresApproveReset` flags, slippage/buffer settings) on the new instance before or during the swap, since a freshly deployed contract starts from defaults; the swap itself cannot affect already-deposited funds, since these guards hold no position state of their own (all position state lives in Aave's/Morpho's own contracts). **Second-pass audit finding, documented not coded around**: because `_maxSafePortion()`'s ceiling is shared across every reserve/market, and both Aave V3 aTokens (a plain transferable ERC20) and Morpho Blue supply positions (`supply()` permits crediting an arbitrary `onBehalf` address) can be credited to a pool by *any* third party without its consent, an attacker could donate a dust position in a reserve/market the pool already tracks but doesn't actively use, timed while that market happens to be near-fully-utilized — zeroing the whole guard's contribution to *immediate* withdrawals until the dust is cleared or liquidity recovers. A size-based dust tolerance would not meaningfully help (the binding constraint is a liquidity ratio, not the donated position's absolute size); a real fix needs USD-value weighting and a new governance-tunable threshold. Left unaddressed given the low practical severity: the queued withdrawal path never consults this cap at all, and the manager can clear donated dust directly via execTransaction at any time |
| `MorphoBlueContractGuard._handleBorrow()` checked the loan token was pool-supported but not the collateral token, letting a manager/trader borrow a supported asset against collateral this pool never independently vetted (FNA-31) | Morpho Blue's `supplyCollateral()` is permissionlessly callable by anyone for an arbitrary `onBehalf`, entirely outside this guard (no `execTransaction` involved) — so an approved market's collateral leg can carry real balance without ever having passed a pool-level support check, regardless of what this guard's own `_handleSupplyCollateral` enforces on the pool's *own* guarded calls. `_handleBorrow()` now also requires `mgr.isSupportedAsset(mp.collateralToken)`, matching every other collateral-touching handler (`_handleSupplyCollateral`, `_handleWithdrawCollateral`, `_handleLiquidate`), which already had this check. Closes the concrete outcome CertiK flagged: `MorphoCollectLib` omits unsupported collateral from NAV and withdrawal planning while still accounting for the (supported) debt, and `afterTxGuard`'s health-factor check reads the collateral's price from the protocol-wide `AssetHandler` registry regardless of pool-level support, so neither would have caught this on its own |
| `docs/deployments.md`'s Asset Guards table recorded the correct guard addresses against the wrong asset type for all four rows, so an operator reconciling the chain against the doc would "correct" a working deployment in the wrong direction (FNA-33, documentation-only, no code change) | Corrected against the live registry (`Governance.assetGuards(uint16)` on Base, confirmed at block 49894684): `MorphoBlueAssetGuard`→Type 1, `AaveV3LendingPoolAssetGuard`→Type 2, `UniswapV3AssetGuard`→Type 3, `ERC20Guard`→Type 4 (Type 0 is unset on-chain, contrary to what the doc previously implied). Types 1-3 match `AssetHandler.sol`'s own top-of-file numbering; the doc now also carries an inline note recording the on-chain source of truth so it doesn't silently drift again. **08/25 follow-up**: CertiK flagged that the same wrong (0/4/5/7) mapping was still live in four other places that a developer or operator could independently reach and act on: each of the four `docs/contracts/*Guard.md` pages' own "Asset Type" header, `Governance.md`'s `setAssetGuard()` parameter table and its separate "Asset Type Registry" table (which additionally claimed a "Type 1 = Closed / non-withdrawable asset" that has no on-chain basis — dropped rather than reassigned, since Type 1 is confirmed `MorphoBlueAssetGuard`), and `developer-guide.md`'s "Registering an Asset Guard" code example, which would have had a developer literally call `setAssetGuard(4, AAVE_ASSET_GUARD_ADDRESS)` — overwriting the real `ERC20Guard` binding at Type 4 with the Aave guard, one of the two "damaging direction" scenarios the original finding specifically warned about. All five corrected to the same Type 1/2/3/4 mapping as `docs/deployments.md`, each now cross-linking to `Governance.md`'s Asset Type Registry as the single canonical table rather than repeating the numbers inline where they can drift independently again |
| Fee-on-transfer / deflationary deposit collateral (FNA-23) | `TokenLogic._deposit()` mints FUSD against the actual balance delta PoolLogic receives, not the caller-supplied nominal amount — closed in code, not just by policy; a mismatch shrinks the minted amount (and trips `minFusdAmount`/`minDepositUSD` if severe) instead of over-minting against collateral the pool never got |
| Fee-on-transfer settlement asset on the queued-withdrawal claim side: `claimCashWithdraw()` trusted the reserved nominal `assetAmount` to be exactly what moved on both the pool's own balance and the claimant's, but a sender-fee/burn-on-transfer asset can drain more than that from the pool's own balance, and a recipient-fee asset can deliver less to the claimant (FNA-23 follow-up) | `FundCalculationLibrary.claimCashWithdrawRelease()` (extracted purely to keep `PoolLogic`'s own bytecode under the EIP-170 limit) measures the actual balance delta. For the normal, post-FNA-03 escrowed case, `WithdrawalEscrow.release()` itself measures what the claimant's own balance actually increased by and returns that as `delivered` — a recipient-fee asset delivers less, reported accurately in `CashWithdrawClaimed`, with no `reservedAssetBalance` involved at all. For a legacy (pre-FNA-03-upgrade) request still claimed from the pool's own balance, `reservedAssetBalance[asset]` is decremented by what genuinely left the pool, not the nominal amount — a sender-fee token draining more than expected would otherwise leave it overstated relative to the real on-chain balance, tripping the legacy `InvalidReservedBalance` invariant on every later guarded transaction and finalization for as long as that overstatement persisted. `fusdNetForAsset` is still burned in full regardless of what the claimant actually received in either case: the reserved value genuinely left the pool (or escrow) either way, whether it landed with the claimant or was consumed by the asset's own fee mechanism en route — refunding the shortfall as unburned fUSD would create a claim no longer backed by the (already-departed) reserved asset |
| Negatively rebasing deposit collateral | Not separately guarded, and doesn't need to be: NAV (`PoolManagerLogic.assetValue()` → `assetBalance()` → asset guard's `getBalance()`) is computed from each asset's live on-chain balance every time, never a cached deposit total, so a rebase down is reflected in fund value automatically and immediately, the same way a price drop is — governance should still avoid whitelisting such tokens, since a sudden large rebase reads identically to a price crash for anyone withdrawing at that moment |
| Rebasing settlement asset between a queued withdrawal's `finalizeCashWithdraw()` and its `claimCashWithdraw()` | Not separately guarded — a rebase changes the token's own raw-unit representation of a fixed USD value between when `reservedAssetBalance`/`assetAmount` are fixed (finalize) and when they're paid out (claim), which no balance-delta measurement at claim time can detect or correct after the fact. Governance must not whitelist a rebasing token as a withdrawal settlement asset; this is the same operational discipline already relied on for deposit collateral above |
| A sender-fee/burn-on-transfer settlement asset draining more than one request's own reserved amount out of `WithdrawalEscrow`'s shared per-pool balance, starving a *different*, still-open finalized claim for the same asset (FNA-23, 4th round) | Not separately guarded, and not fixable by measuring deltas the way the deposit and single-claim cases above are: `WithdrawalEscrow` is deliberately ledger-free by design (its own `balanceOf(asset)` *is* the ground truth of what's reserved for its one pool across *every* outstanding request on that asset, not earmarked per-request — see the FNA-03 entry below). Closing this in code would mean replacing that ground-truth-by-balance model with either a per-request/per-asset outstanding-claims ledger, or a pro-rata redemption scheme paying each claim `escrow's actual balance × (this claim's share / total outstanding claims)` — both real architectural changes to `PoolLogic`/`WithdrawalEscrow`/`FundCalculationLibrary`, all three already tight on EIP-170 bytecode headroom (`viaIR: true` was already required just to fit the FNA-03 escrow fix). Governance must not whitelist a fee-on-transfer/burn-on-transfer token as a withdrawal settlement asset — the same operational discipline already relied on for the rebasing case directly above |
| A positively rebasing/interest-accruing deposit asset whose `balanceOf` for the pool's *pre-existing* holdings jumps as a synchronous side effect of a deposit's own `safeTransferFrom` call, so `TokenLogic._deposit()`'s balance-delta measurement credits (and mints fUSD against) rebase yield that belonged to existing pool holders, not the depositor (FNA-23, 4th round) | Not separately guarded, and structurally can't be by the balance-delta technique alone: delta-around-transfer measures the pool's *total* balance change, which cannot distinguish "moved in from the payer" from "gained via the asset's own ambient rebase on the recipient's pre-existing balance during that same call." This is a sharper version of the general "no rebasing" trust assumption above — governance must not whitelist a rebasing/interest-accruing-`balanceOf` token as a deposit/collateral asset at all, not just because of the down-side case already covered above, but because the up-side case can misattribute yield on every single deposit while looking, on-chain, like an ordinary, correctly-measured transfer |
| fUSD listed as a supported deposit/collateral asset of its own backing pool, leaving fUSD reserved for finalized-but-unclaimed withdrawals unring-fenced from the pool's general fUSD balance and spendable by ordinary guarded manager/trader operations (FNA-23, 4th round) | Not the deployed configuration today (no deploy script or supported-asset list ever references fUSD as its own pool's asset), but nothing on-chain previously prevented it. `PoolManagerLogic._addAsset()` now reverts `CannotAddFusdAsAsset()` if the asset being added equals `IPoolLogic(poolLogic).fusd()`, mirroring the existing `CannotAddPoolAsset()` self-referential guard pattern for other pools' share tokens — closed in code, not just by policy |
| ERC-777-style transfer hooks on deposit/collateral tokens (FNA-23) | `nonReentrant` (see Cooldown Protection / role matrix above) on every deposit, stake, unstake, withdraw, and claim entrypoint in `TokenLogic`, `PoolLogic`, and `FrgmntUserActions` blocks a hook from reentering any of those functions mid-transfer; a hook token could still waste its own gas trying |
| A stake-burn-unstake cycle could clear a user's cooldown timestamp while the bulk of their cooldown-bearing fUSD principal was still parked as a staked (sfUSD) position, letting them bypass the withdrawal cooldown almost entirely (FNA-27) | PoolLogic is cooldown-exempt as both sender and recipient, so staking/unstaking never runs the ordinary cooldown-transfer check. Staking a user's *full* balance already left `cooldownTimestamp` untouched (an existing guard skips the clear whenever the recipient is exempt) — but staking all-but-1-wei (leaving `cooldownPrincipal` briefly nonzero, so that guard never fires) and separately calling the public `burn()` on the dust reached zero principal through burn's implicit recipient, `address(0)`, which is never exempt — clearing the timestamp even though the rest of the principal was still staked. Since a plain transfer-in (unstaking included) never re-sets `cooldownTimestamp`, the returned principal then arrived with no cooldown tracked at all. `TokenLogic._update()` now also checks the user's staked balance in PoolLogic (a low-level staticcall with a fail-open fallback, since `poolLogic` can legitimately be unset at deploy time per FNA-09) before clearing `cooldownTimestamp` to 0 — the timestamp is only cleared once the user genuinely holds nothing staked either. Scoped to PoolLogic specifically, the only address this contract ever marks cooldown-exempt automatically; a governance-added exempt address via `setCooldownExemptSender`/`Recipient` is a separate, deliberate admin decision outside this check |
| Fee-on-transfer / rebasing / hook-bearing tokens as a Uniswap V3 position asset or a Morpho market/vault underlying | Not defensible in code the way collateral deposits are — `UniswapV3AssetGuard`'s `collect()` pays the withdrawing recipient directly from the Uniswap pool (bypassing this repo's own balance-delta accounting entirely), and Morpho's own internal share accounting is out of this protocol's control. Governance must not whitelist such a token for these integrations; `addAssetCheck()` has no on-chain way to detect the behavior ahead of time |
| Aave V4 Spoke Merkl/Points supply incentives unclaimable, risking expiry after `claimUntil` (FNA-19) | `MerklRewardClaimGuard`, registered against Merkl's Distributor, lets the manager/trader claim any Merkl-sourced reward (Morpho Blue, Aave V4 Spoke, or any future Merkl-integrated protocol) via `PoolLogic.execTransaction()`. A claimed `payoutToken` lands in PoolLogic's balance but is **not automatically counted toward NAV** — like any other ERC20 the pool holds, it only contributes to `totalFundValue()` once governance separately adds it as a supported asset via `changeAssets()` (which registers `ERC20Guard` for it and gives it a price feed); until then it sits unvalued but safely custodied |
| A price move on a finalized-but-unclaimed queued withdrawal's reserved liquidity misread as pool yield (FNA-17) | `claimCashWithdraw()` runs fee/reward accrual (`updateFeesAndRewards`) *before* releasing `reservedAssetBalance` and transferring the asset out. Accrual now sources NAV from `FundCalculationLibrary.activeTotalValueWithCompleteness()`, which excludes each asset's reserved balance before pricing it — a finalized request's fixed payout amount can no longer be double-counted as pool yield between finalize and claim. `calculateAvailableManagerFee()`'s preview uses the same reserved-excluding NAV so it always matches what accrual would actually mint. CertiK's follow-up flagged a distinct angle: excluding reserved balance from accrual NAV drops active NAV the moment a request is finalized, but `accountedAssets` (a high-water mark) didn't move to match — organic yield earned while the request sat unclaimed was suppressed behind that artificial overhang and only recognized after claim, letting a late, dominant staker who entered during the delay capture yield earned by earlier holders. Resolved by the same fix as FNA-26 below: `finalizeCashWithdraw()` now syncs `accountedAssets` to active NAV immediately when the reservation is created, so there is no overhang left for organic yield to hide behind — see FNA-17's own test file for a reproduction of CertiK's exact "late staker snipes yield" scenario, asserting the opposite of its demonstrated outcome. CertiK's original FNA-17 write-up separately warned that manager fees could be minted against reserved value leaving the pool later in the same transaction; since performance fee is derived from the identical (active NAV − `accountedAssets`) quantity, it is protected by the same fix — verified directly (not just inferred) with a dedicated test asserting the fee is neither minted against the overhang nor skewed by a later staker's entry, only correctly sized against the real organic yield at the moment it's genuinely recognized |
| A haircut-finalized queued withdrawal's `claimCashWithdraw()` subtracted the claim's full, pre-haircut `fusdNetForAsset` from `accountedAssets`, even though `finalizeCashWithdraw()`'s reservation had already removed only the smaller, haircut-adjusted asset value from active NAV — once `accountedAssets` had independently caught back up to active NAV (e.g. via later, unrelated deposits), this over-decrement understated `accountedAssets` below true NAV by the haircut gap, and a later accrual call misread that gap as yield, letting a staker `harvest()` newly-minted FUSD backed by nothing (FNA-26) | `finalizeCashWithdraw()` now measures the actual active-NAV delta caused by creating the reservation (a before/after read around the `reservedAssetBalance` update) and applies that exact delta to `accountedAssets` immediately, rather than deferring an approximation to claim time. `claimCashWithdraw()` no longer touches `accountedAssets` at all — releasing a reservation and transferring the same amount out leave active, reserved-excluding NAV exactly unchanged (the outflow and the reservation release cancel algebraically, regardless of price movement between finalize and claim), so no further adjustment is backed by any real NAV movement at that step |
| A finalized withdrawal's `reservedAssetBalance` is not physically segregated — a later guarded manager/trader transaction could deplete the same tokens elsewhere, leaving the finalized claim unbacked (FNA-03) | Two earlier rounds patched this via bookkeeping alone: `PoolTxExecutor.exec()`'s `_checkReservedBalancesIntact()` post-check (reverting `InvalidReservedBalance` if `balanceOf(asset) < reservedAssetBalance[asset]`), then `ERC20Guard.txGuard()` additionally capping any single `approve()` to the currently-unreserved balance. CertiK's second follow-up showed both gaps this still left open: (a) *different* spenders can each independently be approved up to the full unreserved amount, with no tracking of their combined pull capacity; (b) an approval granted *before* a reservation existed is never revisited when that reservation is created afterward, so a stale approval can still drain funds now supposed to be protected. CertiK's explicit, twice-repeated recommendation was to stop patching the approval side and physically segregate reserved funds instead. `finalizeCashWithdraw()` now moves the finalized `assetAmount` out of the pool's own balance into `WithdrawalEscrow` — a minimal, non-upgradeable, immutable-`pool`-bound contract deployed once per pool (`initializeWithdrawalEscrow()`, owner-only, one-time) with no internal ledger of its own; its own `balanceOf(asset)` *is* the ground truth of what it holds for that one pool. Once the asset has left, there is nothing left in the pool's own balance for any approval or guarded transaction to reach, closing the class of bug structurally rather than bounding it. `reservedAssetBalance` is deliberately left untouched by an escrowed finalize (it stays 0) — the old post-check and approve-cap remain in place purely for backward compatibility with any request finalized *before* this upgrade, whose `assetAmount` is still sitting in the pool's own balance under the old bookkeeping-only mechanism (`RequestStatus.Finalized`, distinguished from the new `RequestStatus.FinalizedEscrowed` — appended, not inserted, so no previously-stored request's status value shifts meaning). `claimCashWithdraw()` handles both cases: it releases from the escrow when `FinalizedEscrowed`, or from the pool's own balance (measuring the real transfer delta, per the FNA-23 follow-up entry below) when a legacy plain `Finalized` request is claimed post-upgrade. `PoolLogic.sol` compiles via `viaIR: true` (already used elsewhere in this repo for Uniswap V3 periphery contracts) to fit this fix within the EIP-170 bytecode limit — functionally equivalent codegen, not expected to change behavior, verified against the full test suite |
| A failed external position valuation (Aave V4 Tokenization/Spoke, Morpho Vault V2) silently degrades to zero and is consumed as complete NAV, letting fee/reward accrual, deposits, and withdrawals act on an understated total (FNA-04) | `PoolManagerLogic.totalFundValueWithCompleteness()` and `FundCalculationLibrary.activeTotalValueWithCompleteness()` propagate a completeness flag alongside the total; `computeYieldAccrual()` gates on it, skipping fee/reward recognition (rather than recognizing too little against an understated total) so a staker who enters during the outage can't later capture value that was earned but invisible before they joined once the failing guard recovers — see the design note on `computeYieldAccrual()`. CertiK's follow-up flagged that `checkpointFeesForDeposit()` returned normally on this same silent skip, and `TokenLogic._deposit()` ignored its low-level call's result entirely, so a deposit (and the new fUSD supply/`accountedAssets` increment it causes) could still proceed during the outage. `checkpointFeesForDeposit()` now reverts `IncompleteNAV` instead of silently skipping when NAV is incomplete, and `TokenLogic._deposit()` inspects the low-level call's returndata — blocking the deposit outright rather than letting it through on a no-op checkpoint. CertiK's second follow-up flagged that this re-revert only special-cased the `IncompleteNAV` selector specifically, so any *other* checkpoint failure (a genuine bug or panic elsewhere in the fee/reward pipeline, unrelated to NAV completeness) was still silently swallowed the same way the original, fully-ignored low-level call once was. `TokenLogic._deposit()` now bubbles up *any* non-empty returndata verbatim (a minimal, standard `assembly { revert(...) }` re-throw, the only way to forward an arbitrary revert reason in Solidity) rather than comparing against one specific selector. Fails open only in the one case with genuinely empty returndata: a not-yet-upgraded PoolLogic proxy whose old implementation has no matching selector and no fallback, which reverts with no returndata at all — PoolLogic and TokenLogic are separately upgradeable proxies, so this cross-proxy upgrade-ordering case must stay tolerant, deliberately not a typed `try`/`catch`: Solidity's extcodesize guard on a typed external call reverts before the `catch` clause runs whenever the target has no code, which would have turned every deposit into an unconditional revert in precisely the case this call must stay tolerant of. A pool that hasn't called `initializeAutoCompounding()` yet still returns normally (does not revert) from `checkpointFeesForDeposit()` itself, so it never reaches this returndata check at all. **This is also the fix for FNA-22's follow-up**: CertiK filed the identical "checkpoint's low-level call result ignored, deposit proceeds on a no-op" concern separately under FNA-22, and this same `checkpointFeesForDeposit()`/`TokenLogic._deposit()` change closes it — re-verified independently against FNA-22's own wording (result-checked, and the incomplete-NAV case fails closed rather than silently no-op'ing) with both directions covered by `TokenLogic.test.ts`'s "incomplete NAV blocks the deposit checkpoint" suite (including a dedicated test proving an unrelated, non-`IncompleteNAV` checkpoint failure is now bubbled up too) and `PoolLogic.test.ts`'s "FNA-04 follow-up: checkpointFeesForDeposit fails closed on incomplete NAV" suite. CertiK's 08/25 follow-up on FNA-22 restated the same residual concern generically ("non-`IncompleteNAV` checkpoint failures are still ignored, allowing deposits to proceed without updating `lastFeeMintTime`") and asked that the deposit only proceed once the checkpoint "actually advances the accounting state." Auditing every way `checkpointFeesForDeposit()` can return without reverting turns up exactly one: `compoundedRewardIndex == 0` (autocompounding never initialized on this pool). This is provably NOT the FNA-22 bug: `_accrueYield()` itself opens with `_requireAutoCompoundingInitialized()`, so stake/unstake/harvest — every other caller — already revert outright rather than silently skip in this same state, meaning no fee of any kind (deposit-triggered or otherwise) can accrue while `compoundedRewardIndex == 0`; there is nothing for a later accrual to retroactively apply across. Kept deliberately fail-open here anyway, rather than fully complying with CertiK's literal ask to block the deposit outright: doing so would mean no deposit could ever succeed against an already-deployed, not-yet-migrated pool until its owner calls `initializeAutoCompounding()` — a real availability regression purely to guard against a state that provably cannot produce FNA-22's actual failure mode. Closed via a documentation/comment update only (`checkpointFeesForDeposit()`'s doc comment now states this invariant explicitly) plus a new `PoolLogic.test.ts` regression test proving both halves of it (the no-op leaves `lastFeeMintTime` untouched, and `harvest()` reverts `AutoCompoundingNotInitialized` in the same state) — no functional code change, matching the FNA-13 precedent for a Discussion-severity finding resolved via documentation once the underlying behavior is confirmed safe |
| `FundCalculationLibrary._withdrawableFundValue()` (used by both immediate withdrawal and `finalizeCashWithdraw()`) returns only a scalar NAV, unlike `totalValueWithCompleteness()`/`activeTotalValueWithCompleteness()` — a fault-isolated position failing open to a balance of 0 (FNA-04) silently understated it rather than signaling the reading was untrustworthy, so a withdrawal's payout (and specifically its solvency haircut) could be sized against — and a queued request's `assetAmount` permanently fixed against — an understated NAV, misreading a transient guard failure as insolvency (FNA-32, "Second" consequence) | `computeImmediateWithdrawPortion()` and `computeFinalizeAssetAmount()` now both check `totalValueWithCompleteness()` first and revert `IncompleteNAV` if the reading is incomplete, mirroring `checkpointFeesForDeposit()`'s existing FNA-04-follow-up fix for deposits. Unlike stake/unstake/harvest's yield *recognition* — which only defers and is an intentional, accepted design tradeoff (see the FNA-13 design note on `_accrueYield()`, and the "First" consequence below) — a withdrawal's payout is computed and delivered right now and cannot be corrected retroactively once the failing guard recovers, the same reasoning that already made deposits fail closed here. Implemented entirely inside `FundCalculationLibrary` (zero added bytecode to `PoolLogic`, which has essentially no headroom left) |
| FNA-32's "First" consequence: stake()/unstake()/harvest() remain available during incomplete NAV (unlike deposit and withdrawal above), so a user can stake while NAV is incomplete and later share in a "catch-up" recognition of value that existed but wasn't yet indexed, once the failing guard recovers | Deliberately unchanged — this is the same accepted tradeoff as the FNA-13 design note on `_accrueYield()` (rewards belong to whoever holds sfUSD at recognition time, regardless of how briefly, by design — matching fee-less, lock-free ERC-4626-style vaults generally), just triggered by a transient guard failure rather than FNA-13's original example (an unconverted incentive token). Blocking stake/unstake/harvest during incomplete NAV instead would freeze withdrawal of *existing* value over one guard's transient failure — the exact freeze `computeYieldAccrual()`'s fail-open design was built to avoid — a strictly worse outcome than the accepted "recognition can lag, and whoever holds at recognition time benefits" tradeoff already in place |
| An attacker holding a pool's only (dust) effective sfUSD supply could donate directly to PoolLogic and repeatedly harvest, compounding `compoundedRewardIndex` without limit until a checkpoint overflowed `Math.mulDiv`, freezing every stake/unstake/withdraw/harvest (FNA-06) | `_accrueYield()` skips compounding entirely below 1e18 effective sfUSD supply (closes the dust-supply attack outright — the skipped yield is absorbed into `accountedAssets`, not lost) and caps a single checkpoint's applied yield to at most `effectiveSupply * 1e6`, bounding the worst-case growth factor per checkpoint. An earlier revision of this fix also hard-stopped all compounding once the index reached 1e30 as a second safety margin; CertiK's follow-up flagged that this permanently and silently killed all future reward distribution once crossed — including a pool's own long-run organic growth, not just an attack — with no revert to signal it. That ceiling has been removed: the per-checkpoint growth cap alone already bounds the number of checkpoints needed to approach `type(uint256).max` to roughly ten, and each one must independently inject real recognized yield of roughly `effectiveSupply * 1e6` — a cost that scales with the pool's actual staked supply and is economically irrational once that supply is healthy (not sitting at the 1e18 floor). Residual, accepted risk, per CertiK's own recommended tradeoff: an attacker willing to burn capital that scales with (and ordinarily dwarfs) the pool's own liquidity could still force a `Math.mulDiv` revert on some future checkpoint — mitigated operationally (seed real initial liquidity at deploy, keep effective supply away from the 1e18 floor), not by a second code-level ceiling that reintroduces a permanent, silent reward freeze of its own |
| Deposit/queued-withdrawal math applied to a pre-valued/complex asset guard (Aave V3/V4, Morpho Blue, Morpho Vault V2, Uniswap V3) would mint or transfer against the wrong quantity (FNA-18) | These guards' `getBalance()` already returns a fully priced USD-18 figure (see `IPreValuedAssetGuard`); their registered price is a fixed $1 identity multiplier, not a real per-share/per-token price, so `TokenLogic._deposit()`/`fusdToAssetAmount()`/`computeFinalizeAssetAmount()` treating price/decimals as literal per-raw-unit conversion factors would be fundamentally incompatible — e.g. a $2 Morpho Vault V2 share would transfer double the shares a queued withdrawal should deliver. `PoolManagerLogic._addAsset()` now reverts `PreValuedAssetNotDepositable()` if `isDeposit=true` is requested for an asset whose guard implements `IPreValuedAssetGuard`, enforced at the single authoritative point `isDeposit` is ever set (both at initial `changeAssets()` and if later flipped from `false` to `true`) — such assets remain addable as `isDeposit=false` (NAV-only, manager-directed positions), unaffected |
| `ERC20Guard.txGuard()` approves any spender with a registered contract guard, without independently verifying the spender is a legitimate protocol integration (FNA-01, "Withdrawal Centralization Risk") | Reduces to Governance/factoryOwner centralization risk, not a distinct code-level gap — the guard registry itself (`Governance.contractGuards`) is only writable by `Governance.owner`, which (along with `PoolManagerLogic.factoryOwner`, which can replace Governance entirely) is now transferable to the Timelock via `scripts/transferRoles_Governance.ts`; see Centralization Risks above |
| `AssetHandler.initialize()` leaves the deployer key, not `GOVERNANCE_SAFE`, as owner (FNA-01) | `deploy_core_contracts.ts` now transfers `AssetHandler` ownership to `GOVERNANCE_SAFE` immediately after deploy |
| `Timelock.sol` deployed but never assigned as owner/admin of anything, despite its own documented intent (FNA-01) | `scripts/transferRoles_Governance.ts` transfers `Governance.owner`, `AssetHandler.owner`, `PoolManagerLogic.factoryOwner`, and `PoolLogic.owner` to the Timelock, and moves `TokenLogic.DEFAULT_ADMIN_ROLE` to it via grant-then-revoke, as an explicit final deploy step run once bootstrap setup (asset/contract guard registration, `setup_Token_Logic.ts`) is complete — see Centralization Risks above for the one remaining deliberately-deferred item (the three integration Managers, pending their own deploy script) |
| `AssetHandler.initialize()` had no constructor guard, so anyone could call `initialize()` directly on the raw implementation address (bypassing its proxy) and take ownership of that implementation instance (FNA-28) | `AssetHandler` now has `constructor() { _disableInitializers(); }`, the same OpenZeppelin proxy-bypass protection `PoolLogic`/`TokenLogic` already had. Auditing every other `initializer`-modified contract in the repo for the same gap (rather than only the one CertiK named) turned up the identical omission in `PoolManagerLogic` and `NftTrackerStorage` — both genuinely upgradeable, both live Transparent-proxy deployments on mainnet — so the same constructor was added to both. `Governance` was checked and correctly excluded: it uses non-upgradeable `Ownable` with a plain constructor, not the `initializer` pattern, so it was never exposed to this bypass. Taking over a bare implementation instance this way cannot affect the real proxied contract's storage or funds; the risk is confined to whatever the implementation instance itself can do once "owned" (e.g. as a misleading, look-alike contract at that address) |
| `AaveV3LendingPoolAssetGuard`'s multi-hop Uniswap V3 routes (`uniV3PathExactIn`/`uniV3PathExactOut`) were bound by an unrelated, separately-configured single-hop fallback fee (`uniV3Fee`) instead of the route's own fee, so a configured route with zero price impact could still violate its own `amountOutMinimum`/`amountInMaximum` — or, the other direction, be permitted more execution loss than the route actually risks (FNA-29) | `_buildOneCollateralToSettlementSwapTx` (exact-input) and `_buildOneSettlementToDebtSwapTx` (exact-output) now decode the actually-selected multi-hop path via a new `_decodeUniV3Path` helper, validate its endpoints genuinely match the pair it's stored under, and derive the swap bound from the route's own aggregate fee — hop fees compound multiplicatively (product of `1 - fee_i`), not by summing, matching what a real, zero-price-impact swap along the route actually loses to LP fees. Falls back to the single-hop `uniV3Fee` only when no multi-hop path is configured for that pair, reproducing the pre-fix behavior exactly for single-hop routes. `_estimateFlashAmountInSettlement` (flash-loan sizing) shares the same route-fee resolution as the settlement->debt builder it feeds, since sizing the loan against the old, possibly-lower fallback-fee estimate while the actual swap now enforces the correct, possibly-higher route-derived bound could under-fund that swap. An owner-misconfigured path whose endpoints don't match its own mapping key now reverts explicitly at swap-build time rather than silently computing a bound against the wrong route |
| An Aave V4 Tokenization vault's underlying token could be removed from a pool's supportedAssets while the pool still held vault shares wrapping that exact token — nothing linked the vault-share position back to its underlying's own removability check (FNA-20) | `AaveV4TokenizationAssetGuard.addAssetCheck()` now additionally requires the vault's underlying to already be a supported asset of *this* pool specifically (global AssetHandler pricing alone doesn't prove that), and a new `removeTokenCheck()` override blocks removing a token the pool still holds vault shares wrapping, closing both the onboarding-side and removal-side gap. CertiK's follow-up flagged that `removeTokenCheck()`'s catch branch — reached when the vault's own `asset()` call reverts — originally failed *open* (permitted removal) specifically to avoid one broken vault permanently blocking removal of every other supported asset in the pool; but that let a live underlying be removed while a vault that might still recover kept wrapping it, so a later successful `redeem()` would land funds outside any tracked/valued asset, silently understating NAV until governance manually re-added the token. Reversed to fail *closed*: `asset()` is a trivial, near-always-immutable view getter on a correctly-implemented ERC-4626 vault, so a revert there is a strong signal of genuine breakage, not routine transient failure — and the DoS this reopens is narrow and pool-scoped (blocks removing *other* assets from *this* pool only while *this* vault both holds a stuck nonzero balance and stays broken), a bounded cost preferable to silently corrupting NAV accounting for every depositor |
| Both `computeImmediateWithdrawPortion()` and `computeFinalizeAssetAmount()` sized their solvency-haircut `totalClaims` denominator off `fUSD.totalSupply()` alone, omitting reward fUSD the protocol is already committed to minting via `harvest()` but hasn't minted yet (`totalRewardAccrued - totalRewardHarvested`) — a real outstanding claim against the pool just like any staker's already-minted balance. A withdrawer could exit at a more favorable (less haircut) ratio than fair, leaving insufficient real backing for a staker's legitimate, already-earned-but-unharvested reward once they later harvest it (FNA-34) | Both functions now add `IPoolLogic(pool).totalRewardAccrued() - totalRewardHarvested()` into `totalClaims` alongside `totalSupply()`. Fixed identically in both the immediate and queued paths — fixing only one would simply move the exploit to the other withdrawal mechanism, since either lets a withdrawer dodge a fair share of the pool's real outstanding claims. `PoolLogic` already exposed both counters as public state (added for reward-index bookkeeping); this only required surfacing them on `IPoolLogic` for `FundCalculationLibrary` to read. Verified end-to-end in `PoolLogic.test.ts` (a real staker's organic, unharvested reward correctly reduces a separate withdrawer's payout) and in isolation in `UtilityLibraries.test.ts` for both functions directly |
| `AaveLendingPoolAssetGuard.getBalance()` reports gross collateral-minus-debt equity, with no deduction for the flashloan premium, route fees, slippage, rounding, or configured buffer this guard's own unwind route requires to actually realize it. `computeFinalizeAssetAmount()` sizes a queued withdrawal's fixed payout from this gross figure without ever executing the unwind to verify it's realizable, so a queued claimant can be paid in already-liquid assets sourced from Aave equity that isn't actually free to move, leaving remaining holders backed by a disproportionately illiquid, unwind-cost-bearing residual (FNA-35) | New `IUnwindCostAwareGuard` marker interface (mirroring the existing `IWithdrawableBalanceGuard`/`IPreValuedAssetGuard` low-level-call-marker pattern): a guard implementing it exposes `getNetRealizableBalance()`, which `FundCalculationLibrary._withdrawableFundValue()`'s non-liquidity-capped branch now calls instead of raw `getBalance()`, applied identically to both `computeImmediateWithdrawPortion()`'s solvency-haircut NAV and `computeFinalizeAssetAmount()`'s — an immediate withdrawal's own real unwind execution already self-corrects its final delivered amount, but the haircut ratio it's computed from should still reflect true realizable NAV. `AaveV3LendingPoolAssetGuard` now implements this marker: `getNetRealizableBalance()` reuses the guard's own existing flashloan-sizing internals (`_collectDebtPlans()` at 100% portion, `_chooseSettlementToken()`, `_estimateFlashAmountInSettlement()` — the same route-fee/slippage-aware sizing the real unwind transaction itself uses) to estimate the full flash-loan amount needed, then deducts Aave's own `FLASHLOAN_PREMIUM_TOTAL()` on that amount from gross equity, floored at 0. A guard without the marker (everything else) is unaffected, identical to today's behavior. Does not address the separate, broader "which leg of NAV backs a specific settlement asset" allocation question raised by the PoC's remaining-liquidity imbalance — only the narrower, explicitly-recommended fix of not counting unrealizable gross equity at face value. **CertiK's 08/31 follow-up** (PoC re-run): flagged that only the premium was deducted — the route fee and oracle slippage tolerance that `_estimateFlashAmountInSettlement` already computes into `flashAmount` were used solely to size the flashloan, never subtracted from the reported value, so a *cross-asset* debt position (collateral and debt in different tokens, requiring a real settlement->debt swap) still overstated realizable value by the swap's entire cost; the original PoC happened to use a same-asset position where the premium alone was the whole cost, masking the gap. Fixed by pricing the *whole* flashloan outlay (`flashAmount + premium`, in settlement-token terms) and comparing it against `totalDebtInUsd` (the debt's own fair USD value, already subtracted from gross) rather than isolating each cost component separately — `flashAmount` is provably always >= the fair settlement-equivalent of the debt being repaid (same-asset legs contribute their exact repay amount pre-buffer; cross-asset legs are already `_oracleMaxIn`-inflated by slippage/fee pre-buffer; the configured buffer then multiplies the whole sum), so the excess captures buffer + route fee + slippage + premium together in one conservative calculation — closing the original recommendation's full ask ("premium, route fees, slippage, rounding, and configured buffers"), not just the narrower 08/31 comment. Two new tests added: a same-asset case proving the buffer is now captured (previously silently ignored even without any cross-asset swap), and a cross-asset case reproducing the exact CertiK gap — hand-derived expected values (mirroring `_oracleMaxIn`'s exact fee-gross-up formula) matched the contract's output exactly, including the precise $16 gap between the old premium-only formula and the corrected one |
| `PoolLogic._withdrawProcessing()` computes each supported asset's own `portionBalance` during an immediate pro-rata withdrawal, but never used it to decide whether to call that asset's guard at all — a leveraged Aave position whose reported equity is zero (debt ≥ collateral) still returned a real flashloan-unwind transaction purely because debt existed. If that doomed unwind failed, the revert took down the *entire* withdrawal, including every other, healthy asset's share, even though the zero-equity leg itself contributed nothing to the NAV the withdrawal was sized against (FNA-36) | `_withdrawProcessing()` now sizes `v.balance` via the same `FundCalculationLibrary.guardNetRealizableBalance()` used for NAV (FNA-35's `IUnwindCostAwareGuard` marker check, now exposed as a `public` library function PoolLogic also calls directly) and skips the asset entirely — no guard call, no planned transaction, `(address(0), 0, false)` — whenever the resulting `portionBalance` is zero. This is guard-agnostic (matches the recommendation's general "skip when balance or portionBalance is zero" ask) and also correctly skips an Aave leg whose *gross* equity is thin-but-positive yet fully consumed by unwind costs, since that case already reduces to zero net-realizable value through the same FNA-35 marker. Costs ~102 bytes of `PoolLogic`'s already-thin EIP-170 headroom (934 → 832). **CertiK's 08/31 follow-up**: confirmed the zero-equity case is closed, but flagged that the "rather than needing a second, Aave-specific solvency check inside `withdrawProcessing()`" reasoning above doesn't hold — `AaveV3LendingPoolAssetGuard.withdrawProcessing()` itself still returned a flashloan transaction whenever debt existed, with no validation that *this specific* unwind (at the actual, possibly liquidity-capped `effectivePortion` being planned, not the 100% figure `guardNetRealizableBalance()` evaluates) is solvent after the same swap fees, slippage, rounding, and buffers, sharing its root cause with FNA-35's `getNetRealizableBalance()` gap. Fixed by adding an explicit solvency gate directly inside `withdrawProcessing()`, immediately after `flashAmount` is sized and before any transaction is built: prices the full flashloan repayment obligation (`flashAmount + premium`, in settlement-token terms) and compares it against collateral value at `effectivePortion` (linear in portion, the same scaling assumption this guard's debt/flashloan sizing already relies on throughout); if collateral doesn't cover it, fails closed — `(address(0), 0, new MultiTransaction[](0))`, the same shape `_withdrawCollateralAndTransfer()` already returns for an all-dust no-debt case — instead of planning an unwind doomed to revert and taking every other healthy asset's share down with it, exactly the recommendation's explicit "or failing closed for such positions" alternative. Deliberately compares collateral directly against total outlay rather than routing through a subtracted debt-value term: that term cancels out of the comparison algebraically regardless of its exact composition, and `_getBalance()` (variable-debt only) and `_collectDebtPlans()` (stable + variable) account for debt composition differently, so computing it here would only reintroduce that mismatch with no effect on the result. This is independent, defense-in-depth coverage inside the guard itself — it holds regardless of whether a caller is `PoolLogic`'s own upstream `portionBalance` skip or anything else invoking `withdrawProcessing()` directly, and regardless of `effectivePortion` differing from the 100% figure the upstream skip evaluates (e.g. after the FNA-07 liquidity cap reduces it). Three new tests, all hand-derived exact math: a same-asset case (premium + buffer only) that fails closed just below the exact repayment obligation and a companion that still builds the flashloan just above it, plus a cross-asset case reusing FNA-35's own reference numbers (100 USDC + 1 WETH @ $2,000 debt, 0.3% route fee, 0.5% slippage, 1% premium) at collateral where raw debt is covered ($30 positive) but the full $37.16 route-fee/slippage/premium cost isn't, calling `withdrawProcessing()` directly (bypassing `PoolLogic` entirely) to prove the gate is the guard's own property, not just a side effect of the upstream skip |
| `UniswapV3AssetGuard.getBalance()` calls `UniswapV3PriceLibrary.assertFairPrice()` directly for every tracked NFT position, which `require()`s the pool's live spot price stays within a Chainlink-derived fair-price band and reverts the *entire* `getBalance()` call otherwise. A thin or non-mainstream Uniswap V3 pool's spot price is cheap for an external trader to push out of band, and the existing `IIncompleteValuationGuard` completeness mechanism can't help — `PoolManagerLogic.totalFundValueWithCompleteness()` calls `assetValue()` (which reverts) before it ever reaches the `_isValuationComplete()` check. The failed NAV read then blocks stake, unstake, harvest, and immediate withdrawal processing for the *entire* pool, not just the affected position (FNA-37) | New `UniswapV3PriceLibrary.isFairPrice()`, a non-reverting counterpart to `assertFairPrice()` that returns `(bool inRange, uint160 sqrtPriceX96)` instead of asserting — added as a separate function rather than a shared internal with a "revert or not" flag, since `assertFairPrice()` also guards manager transactions (minting/increasing liquidity at a manipulated price) via `UniswapV3NonfungiblePositionGuard`, where reverting is exactly the intended, unchanged behavior. `UniswapV3AssetGuard` now implements `IIncompleteValuationGuard`: `getBalance()` and the new `isValuationComplete()` share a `_valuePositions()` helper (mirroring `MorphoVaultV2AssetGuard`'s `_valuePosition()` pattern) that skips (contributes 0 for) just the one out-of-band position — the same as the pre-existing unsupported-token skip immediately above it — and reports `complete=false` for it, rather than reverting the whole position loop. `PoolManagerLogic`'s existing marker-detection machinery (unchanged) then correctly threads that into `totalFundValueWithCompleteness()`'s completeness flag |
| A finalized-but-unclaimed queued withdrawal's backing asset is excluded from active NAV from the moment it's finalized (physically escrowed, FNA-03), but its FUSD isn't burned until `claimCashWithdraw()` — so it stayed counted as an active claim in `fUSD.totalSupply()` in the meantime, double-counting the same value and understating every other still-active claim's fair share. This under-haircut hit both `computeImmediateWithdrawPortion()` (an unrelated immediate withdrawer receives less than their fair share) and `computeFinalizeAssetAmount()` (a later queued finalize's `assetAmount` is permanently fixed below its fair share, uncorrectable at claim time) (FNA-38) | New `finalizedUnclaimedFusd` — the sum of `fusdNetForAsset` across every request currently `Finalized`/`FinalizedEscrowed` but not yet `Claimed` — incremented in `finalizeCashWithdraw()` and decremented in `claimCashWithdraw()` only for an escrowed claim (a legacy, pre-FNA-38 plain-`Finalized` request was never added here, so is never subtracted either — a bounded, self-correcting gap: only ever *under*-excludes claims until any such legacy request is itself claimed, never over-excludes). Both functions now compute `totalClaims` via a shared `_activeTotalClaims()` helper that subtracts it. This also corrects a prior doc comment on `computeFinalizeAssetAmount()` that documented the asymmetry as a "KNOWN LIMITATION" for the queued path while explicitly exempting the immediate path on the (incorrect) premise that `ERC20Guard.withdrawProcessing()`'s reservation-netting "cancels" against the NAV-side netting — it doesn't: both numerator and denominator were reservation-netted, so no cancellation occurred and the same asymmetry hit both paths, now fixed identically in both |
| `AssetHandler.setEurUsdAggregator()`/`clearEurUsdAggregator()` could be called at any time to globally switch every registered asset's price between raw-USD and EUR-converted quotes. Existing FUSD supply, `PoolLogic.accountedAssets`, reward baselines, and pending queued withdrawal requests are never migrated when the mode changes — a pure conversion-basis change can be minted as pool yield (performance fees/staker rewards with no real collateral appreciation), and a queued request can be finalized for a different token amount than the basis in effect when it was created, or revert if the recomputed amount exceeds available liquidity (FNA-40) | New `eurUsdModeLocked`: the first call to either function now permanently locks the valuation basis — both functions revert `"Frgmnt: eur/usd mode locked"` on every subsequent call, for the life of the contract. Matches the existing, already-one-time deploy-script usage exactly (`setEurUsdAggregator()` is called once, immediately after `AssetHandler` deploy, before any pool exists) — `deploy_core_contracts.ts` now also calls `clearEurUsdAggregator()` once for a USD-product deployment (previously never called at all), explicitly locking in raw-USD mode from genesis instead of leaving it toggleable forever. **Live-mainnet note**: the already-deployed, already-live USD `AssetHandler` predates this fix and was never locked at its own deploy time — after this fix is included in that contract's next upgrade, governance must separately call `clearEurUsdAggregator()` once (a harmless no-op on the aggregator itself, which was never set) to actually lock in USD mode for that live deployment; until that call is made, the vulnerability this fix closes remains open for it specifically |
| `MorphoVaultV2AssetGuard._valueShares()` and `AaveV4TokenizationAssetGuard._valuePosition()` treated a share balance that successfully converts to zero underlying units the same as a genuinely failed valuation — `(0, false)`, "incomplete" — even though both external calls succeeded and the answer is a real, known zero. Any transferable-share holder could donate a small enough nonzero share amount directly to the pool, with no Frgmnt guard or pool approval needed, to trigger this: `PoolManagerLogic.totalFundValueWithCompleteness()`'s incomplete flag then made deposit checkpointing, immediate-withdrawal sizing, and queued-withdrawal finalization revert pool-wide, and the dust position couldn't be cleared through the normal removal path either, since `removeAssetCheck()` requires the raw share balance to be exactly zero (FNA-41) | Both guards now return `(0, true)` for this case instead of `(0, false)` — matching the pre-existing `shares == 0` early return one line above it in each function, and matching how `AaveV4SpokeAssetGuard._valueAmount()` already treated a zero amount. Every other `(0, false)` in these functions is a genuine failure to obtain an input (a reverting call, a missing underlying, an unpriced asset); this was the one case where every input was obtained successfully and the result is a valid number, not an unknown one — reporting it as unknown froze the pool over a dust value the pool guards against by definition. Also correctly handles a vault that genuinely depreciates to zero: `(0, true)` reports the real loss and lets the pool keep operating, where `(0, false)` would freeze it indefinitely instead |
| `accountedAssets` is a high-water mark — `_accrueYield()` only ever ratchets it UP to match a new, higher NAV, never down to follow a loss, so it can sit above active NAV (an "overhang") for a genuinely unrecovered loss. Both `PoolLogic._withdrawCashImmediateToSafe()` and `finalizeCashWithdraw()` retired a claim's FUSD but reduced `accountedAssets` by only `valueDelta` (the real dollars that left) — correct only when there is no overhang, but whenever `accountedAssets > active NAV`, the exiting claim's own proportional share of that overhang was realized (permanently gone, its FUSD burned/transferred) without ever being removed from the baseline, permanently overstating `accountedAssets` for the *remaining* claims and delaying/suppressing their yield recognition after every underwater exit (FNA-42) | New shared `FundCalculationLibrary._computeAccountedAssetsReduction()`, used by both withdrawal paths: reduces `accountedAssets` by `valueDelta + realizedLossShare`, where `realizedLossShare = netFusd * overhang / totalClaimsBeforeWithdrawal` and `overhang = max(accountedAssetsBefore - completeFundValue, 0)`. This keeps `(accountedAssets - activeNAV) / totalClaims` constant across any partial claim retirement — proportional or not, haircut-triggering or not — verified as an explicit invariant in both a library-level unit test and a full `PoolLogic` integration test; when there is no overhang, `realizedLossShare` is 0 and behavior is unchanged from before. `completeFundValue` (not PoolLogic's own liquidity-capped `_withdrawableFundValue()`) is deliberately reused as the "NAV before" reference for the overhang term — the same reserved-excluding, net-realizable figure the solvency haircut is already computed against — since the liquidity-capped figure can sit below `accountedAssets` purely from a temporary per-asset liquidity constraint (FNA-07) with no real loss at all, which would misread illiquidity as an overhang and over-reduce the baseline for no reason. Extended to the queued path too (`finalizeReserveAndUpdateBaseline()`) though the finding cited only the immediate path, since it has the textually identical gap and the recommendation explicitly asked for one consistent treatment across "active NAV, outstanding claims, reserved withdrawals, and the baseline" — fixing only one path would just move the exploit to the other. Costs ~233 bytes of `PoolLogic`'s already-thin EIP-170 headroom (756 → 523) |
| `PoolManagerLogic.commitFeeIncrease()` settles manager fees via `mintManagerFee()` before installing the newly-announced (higher) fee numerators, but `mintManagerFee()` -> `PoolLogic._accrueYield()` is a deliberate no-op on an incomplete NAV (see FNA-04), leaving `accountedAssets`/`lastFeeMintTime` exactly where they were. If NAV happens to be transiently incomplete at commit time, the new numerators are still installed immediately — so the first *complete*-NAV accrual afterwards charges the new, higher rate on the entire pre-commit interval's yield and elapsed time, retroactively (FNA-43) | `commitFeeIncrease()` now reverts `"NAV incomplete"` if `totalFundValueWithCompleteness()` reports incomplete, before ever calling `mintManagerFee()` or installing the new numerators — the manager must wait for the guard to recover, so the old rate always gets its own checkpoint first. `totalFundValueWithCompleteness()` changed from `external` to `public` so `commitFeeIncrease()` can call it by name from within the same contract (a plain internal-call visibility fix, not the delegatecall-library `this.` pitfall seen elsewhere in this codebase — `PoolManagerLogic` is never delegatecall-invoked). Reuses the exact same `complete` flag `PoolLogic`'s own accrual reads a moment later (`activeTotalValueWithCompleteness()` derives from this same figure, only subtracting reserved value, never touching completeness), so the check is exactly equivalent, one transaction earlier. Deliberately scoped to `commitFeeIncrease()` only — `mintManagerFee()` itself keeps its existing fail-open behavior for its other caller, `changeManager()`, so replacing an underperforming or unresponsive manager is never blocked by an unrelated guard's transient valuation hiccup. `PoolManagerLogic` has ~7.6KB of EIP-170 headroom, so this fix lives entirely there rather than touching `PoolLogic.sol`, whose own headroom is critically thin (523 bytes, see FNA-42) |
| `AaveV4SpokeContractGuard`'s supply/approveWithdraw/withdraw handlers validate that the Spoke is a supported pool asset and that the reserveId is allowlisted/tracked, but never resolve or check the reserve's actual underlying ERC20 — Aave V4 addresses a market as `(spoke, reserveId)`, not by the underlying's own address. A reserve whose underlying is not (or is no longer) a supported pool asset could still be supplied into (given a stale/pre-existing allowance to the GiverPositionManager) and, more directly, withdrawn out of — landing the funds as an idle ERC20 that `totalFundValue()` doesn't iterate, that ordinary pro-rata withdrawals don't distribute (they only process `supportedAssets`), and that the manager cannot even clean up via the normal guarded `execTransaction` path, since `PoolTxExecutor`'s guard-resolution fallback itself requires `isSupportedAsset(target)` before dispatching to the target token's own asset guard (FNA-44) | New `_requireSupportedUnderlying()`, called from both `_requireActiveReserve()` (supply) and `_requireTrackedReserve()` (approveWithdraw + withdraw): resolves the reserve's underlying via the same trusted `getReserve(uint256)` raw-staticcall pattern `AaveV4SpokeAssetGuard._getReserveUnderlyingAndHub()` already uses (duplicated rather than shared — small, self-contained, single-purpose, no state), then requires `IHasSupportedAsset.isSupportedAsset(underlying)`. Reverts (rather than failing open) on a staticcall failure or a zero-address result, matching this contract guard's existing all-`require` posture — gating a fund-moving transaction before it executes is not a passive valuation read. Deliberately does NOT relax for a delisted-but-tracked reserve the way the reserveId check itself does (FNA-10): withdrawing into an unsupported idle balance is strictly worse than leaving the position parked in the still-valued Spoke, so the underlying check applies unconditionally on both the supply and withdraw sides |
| `SlippageAccumulator.assetValue()` multiplies every asset's raw amount by its registered price feed, unlike `PoolManagerLogic.assetValue()`, which short-circuits to the raw amount for a pre-valued asset guard (`IPreValuedAssetGuard`) whose `getBalance()` already returns a fully priced USD figure and whose registered price feed is only a placeholder $1.00 identity aggregator. If a pre-valued ERC-20 share were routed through the guarded Uniswap router, its slippage would be measured against raw share counts instead of economic value — weakening the cumulative-loss bound for a share worth more than $1, or tripping it early for one worth less. Currently latent, not exploitable: of the six guards implementing the marker, four are registered against non-ERC-20 addresses (`IERC20Extended(asset).decimals()` reverts, so the path fails rather than misprices), and the two that mark transferable ERC-20 shares (`MorphoVaultV2AssetGuard`, `AaveV4TokenizationAssetGuard`) are not currently deployed or whitelisted as router endpoints (FNA-45) | `SlippageAccumulator.assetValue()` now applies the identical `isPreValuedAssetGuard()` low-level-staticcall short-circuit `PoolManagerLogic.assetValue()` already uses, looked up via `IHasGuardInfo(poolFactory).getAssetGuard(asset)` — `poolFactory` here is already `PoolManagerLogic`'s own address (a misleadingly-named constructor parameter; see `deploy_contract_guard.ts`'s fix for the earlier pool-factory-mixup finding), which implements `getAssetGuard()`, so no new dependency or parameter was needed. Both consumers of the marker now agree on how a pre-valued balance converts to USD. **CertiK's 09/02 follow-up**: rejected this patch — `PoolManagerLogic.assetValue()`'s shortcut receives an aggregate guard *balance* already denominated in base-currency-18 (correct to return unchanged); `SlippageAccumulator.assetValue()` receives a raw *token amount delta* from a swap, so mirroring the same shortcut there assumed 1 raw unit is always worth exactly $1 — itself a mispricing bug for a real transferable share (Morpho Vault V2 / Aave V4 Tokenization) worth more or less than $1/unit, and additionally a 1e12 scaling error for a hypothetical 6-decimal share. Required both: (1) remove the shortcut, and (2) give pre-valued guards a real per-unit price so removing it doesn't just reintroduce the identity-price bug one level up. Implemented as a new `IPreValuedAssetGuard.getUnitPrice(address) external view returns (uint256)`, which `PoolManagerLogic.getAssetPrice()` now dispatches to (same `isPreValuedAssetGuard()` marker check, then a plain typed call — a guard's revert propagates rather than being swallowed, the correct fail-closed behavior for a price a caller is about to act on). The two transferable-share guards (`MorphoVaultV2AssetGuard`, `AaveV4TokenizationAssetGuard`) implement it for real: value one whole share (`10**realShareDecimals` — `getDecimals()` on both was also fixed, from a hardcoded 18 to the share token's actual decimals, since it serves no purpose for `assetValue()`'s shortcut and was simply wrong for any direct consumer) via the vault's own `convertToAssets()`, priced through the underlying's real `getAssetPrice()`/`assetDecimal()` — reverting (not degrading to 0, unlike `getBalance()`'s deliberately fail-open design) on any failed dependency or a zero result. The four non-transferable pseudo-asset guards (Aave V3/V4 Spoke, Morpho Blue, Uniswap V3) revert unconditionally from `getUnitPrice()` — there is no meaningful per-unit price for a lending/NFT position, and reverting lets `getAssetPrice()`'s dispatch fail closed instead of silently returning a misleading number, for this or any future caller. `SlippageAccumulator.assetValue()`'s own shortcut is removed entirely, exactly per the recommendation — it now prices every asset uniformly through `getAssetPrice()`. Verified every current call site of `getAssetPrice()`/`assetDecimal()` in the codebase either only ever receives a real underlying token (never a pre-valued pseudo-asset or share itself) or is already gated by FNA-18's `isDepositAsset` restriction (which pre-valued guards can never satisfy) — so this dispatch change is additive everywhere except the one path it's meant to fix |
| `MorphoVaultV2ContractGuard` accepted a standalone `forceDeallocate` call. It burns penalty shares from the pool but moves the freed assets into the vault's *shared* idle liquidity, not to the caller — between that call and a separate, later withdraw/redeem `execTransaction` (PoolLogic can't batch these atomically), any other vault shareholder can withdraw against that same liquidity first. `forceDeallocate`'s own precondition (a dry adapter) makes competing exit demand for that liquidity the expected state at exactly that moment, not an edge case, so the pool could pay the penalty and still not get its exit. Bounded and repeatable rather than catastrophic — the penalty is capped by Morpho's own protocol-level maximum and the operation is retryable — but a real fee-leak exposure on this manager-side manual recovery primitive (not part of the automatic `withdrawProcessing()` path). Distinct from the pre-existing, already-documented third-party `forceDeallocate` griefing risk above, where an *outsider* calls it against the pool's position — here the pool calls it deliberately and loses the race for its own result (FNA-46) | `forceDeallocate` is no longer dispatched standalone — a bare call now falls through to `NotUsed`. The only way to reach it is through the vault's native `multicall(bytes[])`, newly decoded by this guard: every leg but the last must be a validated `forceDeallocate` (identical checks to the old standalone handler — `onBehalf == pool`, adapter registered), and the last leg must be exactly one pool-bound `withdraw` or `redeem` (identical checks to their existing handlers). Batching makes the penalty and the exit atomic in one transaction, so no other shareholder gets a window to claim the freed liquidity first. A zero-`forceDeallocate`-leg batch (just one withdraw/redeem) is also permitted — functionally identical to calling that selector directly, so there's nothing to gain from forbidding it; a batch ending in anything other than withdraw/redeem (including another `forceDeallocate`, i.e. "all deallocate, no exit") reverts. New `TransactionType.MorphoVaultV2ForceDeallocateAndExit` (appended at the end of the enum, existing numeric values unchanged) distinguishes the batched flow from the now-unused standalone `MorphoVaultV2ForceDeallocate` type. Mirrors the recursive `txGuard()`-call pattern `UniswapV3RouterGuard` already uses for its own `multicall` support |
| `UniswapV3RouterGuard.txGuard()`/`SlippageAccumulatorUser.afterTxGuard()`'s only access check (`msg.sender == IPoolManagerLogic(poolManagerLogic).poolLogic()`) is circular: `poolManagerLogic` is caller-supplied, so any contract that returns itself from `poolLogic()` (and `true` from `isSupportedAsset()`) satisfies it — there is no trusted pool-registry lookup anywhere in the guard layer (`isPool()` exists on `PoolManagerLogic` but has zero call sites in `guards`/`utils`, and isn't populated by any deploy script in this single-pool-per-deployment fork anyway). `intermediateSwapData` was a single contract-level slot shared by every caller of this singleton guard, and `afterTxGuard()` discards its own `data` argument and re-reads that slot instead of re-deriving from calldata. Combined with `_decodePath()` validating only the first/last token of a multi-hop route (intermediate hops unconstrained), an attacker-forged `poolManagerLogic` call, or a malicious intermediate-hop token that briefly gets control mid-swap, could overwrite the real pool's pending snapshot before its own `afterTxGuard` read it back — masking a real loss past the deployed 5% cumulative-slippage bound, or (a variant needing no privileged actor at all) forcing the checked-arithmetic subtraction in `afterTxGuard` to underflow and revert the honest pool's swap (FNA-47) | `intermediateSwapData` is now `mapping(address => SwapData)` keyed by `msg.sender` (the caller — `poolLogic` for every legitimate call, already checked against `msg.sender` at both write and read time) rather than a single shared slot. This closes both variants structurally without needing a trusted pool registry or per-hop validation: `msg.sender` can never be forged, so an attacker's own forged-`poolManagerLogic` call, or a malicious hop's mid-swap callback, only ever writes to *its own* mapping entry — it can never collide with, be read by, or clear the real pool's entry. This repo's EVM target is paris (pre-Cancun, see FNA-24), ruling out transient storage as an alternative to a mapping. **Live-mainnet note**: both `UniswapV3RouterGuard` and `SlippageAccumulator` are already deployed on Base (see `docs/deployments.md`); `SlippageAccumulator` itself is unchanged, but this fix requires deploying a new `UniswapV3RouterGuard` (same constructor argument, the existing `SlippageAccumulator` address) and governance calling `Governance.setContractGuard()` to point the Uniswap V3 SwapRouter at the new guard — the code fix alone does not protect the currently-live guard instance until that swap happens |
| A Uniswap V3 position fully decreased and collected (but not burned) reports a zero balance from `UniswapV3AssetGuard.getBalance()`, which lets the inherited `ERC20Guard.removeAssetCheck()` (balance-only) permit delisting the NonfungiblePositionManager from `supportedAssets` while its NFT remains tracked in `NftTrackerStorage` — tracker entries are only ever cleared on `burn()`, and `PoolManagerLogic._removeAsset()` never touches the tracker. The position manager's contract guard stays globally registered regardless of per-pool delisting (`PoolTxExecutor._resolveGuard()` only applies the `isSupportedAsset` check when NO contract guard exists — deliberate, the FNA-14 fix for a separate finding), and `increaseLiquidity` was the only branch in `UniswapV3NonfungiblePositionGuard` that never checked `isSupportedAsset` on the position manager or the position's own underlying tokens (unlike its `mint`/`collect` siblings). A manager or trader (delisting only needs `changeAssets`, which a trader can call while `traderAssetChangeDisabled` is false, the default) could therefore decrease+collect a position to zero, delist the position manager via that balance-only check, then `increaseLiquidity` real ERC-20 capital back into the still-tracked NFT — hiding it from `totalFundValue()` and from pro-rata withdrawal enumeration, a bypass of the same "can't remove a nonzero-balance asset" invariant a plain ERC-20 already enforces (FNA-48) | Primary fix: new `UniswapV3AssetGuard.removeAssetCheck()` override reverts `"UniswapV3AssetGuard: positions tracked"` whenever the pool's tracked-tokenId list (resolved via the same `IHasGuardInfo(factory).getContractGuard(asset)` -> `getOwnedTokenIds(pool)` pattern already used by `getBalance()`/`removeTokenCheck()`/`withdrawProcessing()`) is non-empty — closing the whole "delist then refill" family regardless of which guard branch it travels through, not just `increaseLiquidity`. Defense in depth: `increaseLiquidity` now also requires `isSupportedAsset` on the position manager and both underlying tokens, mirroring `mint`'s existing checks exactly — `decreaseLiquidity`/`collect`/`burn` deliberately keep no such check, so an existing position (even a delisted one) can always be unwound. Per the finding's own recommendation, swept every other contract guard (`AaveLendingPoolGuardV3`, `MorphoBlueContractGuard`, `AaveV4SpokeContractGuard`, `MorphoVaultV2ContractGuard`, `UniswapV3RouterGuard`) for the same asymmetric-branch omission — none found: Aave V3 checks both the pool and the moved asset symmetrically on every branch; Morpho Blue checks `isSupportedAsset(to)` once, uniformly, before dispatching to any branch; the Aave V4 Spoke and Morpho Vault V2 guards were already hardened for this exact class of gap by FNA-44/FNA-46 in this same engagement |
| `_getEffectiveSlippage()` (duplicated character-for-character in `MorphoMathLib` and `AaveLendingPoolAssetGuard`) derived a single floor from the pool fee as the exact-output gross-up `fee/(1-fee)` and used it for BOTH `oracleMinOut` (exact-input) and `oracleMaxIn` (exact-output), then took `max(configuredTolerance, feeFloor)` instead of composing them. Two compounding issues: (1) the gross-up form overstates the correct exact-input floor by 1bps at the 1% fee tier (101 vs the correct fee-only 100 — they agree at the 0.05%/0.3% tiers); (2) `max()` meant the operator's configured tolerance (70bps default) was silently discarded whenever the fee floor exceeded it — at the 1% tier, `max(70, 101) = 101`, leaving only ~1bps of real headroom on the forced settlement swap, exactly where a legitimate settlement is most likely to revert (FNA-49) | Split into `_effectiveSlippageExactIn()` (fee-only floor: `fee/1e6`, used by `oracleMinOut`) and `_effectiveSlippageExactOut()` (gross-up floor: `fee/(1e6-fee)`, used by `oracleMaxIn`), each **composing** (adding) the configured tolerance to its own correct floor instead of maxing — the operator's tolerance now survives at every fee tier. Per the finding's own warning, the direction correction and the composition change had to land together: substituting only the fee-only figure without composing would set the exact-input minimum at exactly the zero-impact output and revert every 1% tier swap. Applied identically to both existing copies (kept character-for-character identical, matching their existing relationship) rather than extracting a shared implementation, to keep the fix minimal — re-verified against the finding's own worked 1% (10000 ppm) example numbers (100 vs 101, and 170 vs 171 once composed with the 70bps default) |
| A lending position's `getBalance()` reports gross economic equity, but every guard clamps it at 0 rather than returning a negative number, since every NAV consumer in this codebase sums non-negative `uint256` balances. A position whose debt exceeds its collateral (an unliquidated/under-liquidated Aave or Morpho Blue borrow) therefore contributes exactly 0 to NAV — correctly excluding its own now-worthless equity, but silently *omitting* its deficit rather than *subtracting* it. The borrowed tokens that position produced (now sitting elsewhere in the pool, e.g. swapped into another supported asset) are still counted as if the debt backing them did not exist, overstating `totalFundValue()`, fee/reward accrual (both derive from the same NAV), and every withdrawal-sizing/solvency-haircut computation (`FundCalculationLibrary._withdrawableFundValue()`'s two independent capped/uncapped branches) by the full amount of the omitted deficit (FNA-54) | New `IDeficitReportingGuard` marker interface (`isDeficitReportingGuard()` + `getDeficit(pool, asset) -> uint256`), checked via the same low-level-staticcall-with-graceful-fallback pattern as the existing `IPreValuedAssetGuard`/`IUnwindCostAwareGuard`/`IIncompleteValuationGuard` markers. Implemented by `AaveV3LendingPoolAssetGuard` and `MorphoBlueLendingPoolAssetGuard` (the only two guards whose position can carry genuine negative equity), reporting `debt - collateral` floored at 0 — `MorphoCollectLib` gained a shared `_aggregate()` helper so `getBalance()`/`getDeficit()` compute from the same totals without duplicating the market-iteration loop. Two independent aggregation points sum every asset's reported deficit and subtract it (floored at 0) from the gross total: `PoolManagerLogic._subtractTotalDeficit()` (used by both `totalFundValue()` and `totalFundValueWithCompleteness()`, so the fix propagates automatically to fee/reward accrual and deposit checkpointing) and `FundCalculationLibrary._withdrawableFundValue()` (used by both the liquidity-capped and uncapped withdrawal-sizing branches — a separate, independent per-asset loop that doesn't reuse `PoolManagerLogic`'s aggregation). The small "check the marker, sum deficits" loop is deliberately duplicated across these two rather than shared, since `FundCalculationLibrary` is an externally-linked library and giving `PoolManagerLogic` a new dependency on it would require new deployment-time library-linking wiring — same tradeoff already accepted for FNA-49's duplicated slippage-floor helpers. `AaveLendingPoolAssetGuard.getNetRealizableBalance()` (the `IUnwindCostAwareGuard` path from FNA-35) needed no separate fix, since the new deficit subtraction operates at the aggregate level regardless of which per-asset function contributed the already-zero-clamped balance. **CertiK's 08/31 follow-up**: `FundCalculationLibrary.computeImmediateWithdrawPortion()` correctly sizes `fairFusd` against the deficit-adjusted `completeFundValue` (haircutting it when `totalClaims` exceeds true NAV), but then divided by that *same* deficit-adjusted figure to get `portion` — and `PoolLogic._withdrawProRataInternal()` applies `portion` to each guard's own reported (gross, already-floored-at-0) balance, not a deficit-adjusted one. A guard carrying a deficit never contributes a *negative* balance for `portion` to multiply against, so dividing by the smaller, net denominator inflated `portion` relative to what the gross assets could support — extracting strictly more than `fairFusd` whenever any position was underwater, which then failed `PoolLogic`'s own `valueDelta > netFusd` sanity check and reverted `InvalidFundValue()` on essentially every immediate withdrawal while any deficit existed, not just a rare insolvency edge case. Fixed by reconstructing the gross, liquidity-capped NAV precisely: `FundCalculationLibrary._withdrawableFundValue()` now returns `(grossValue, totalDeficit)` separately instead of pre-netting, and `computeImmediateWithdrawPortion()` divides `fairFusd` by `withdrawableFundValue + totalDeficit` — exact (no extra asset-loop pass) because each guard's `getDeficit()` query doesn't depend on the liquidity-capping dimension, so the same `totalDeficit` total applies to both the capped and uncapped figures, and the caller-supplied `withdrawableFundValue` parameter is confirmed nonzero (hence not itself deficit-floored) before this reconstruction runs. `computeWithdrawableFundValue()`'s own external behavior (still deficit-adjusted) and `computeFinalizeAssetAmount()`'s queued-withdrawal path (which converts `effectiveFusd` directly via price/decimals, with no portion/NAV-denominator division at all, so it was never exposed to this bug) are both unchanged. New regression test reproduces the finding's own PoC numbers (100 collateral / 90 borrowed-and-held / 80 after the collateral drop) at a claims level where no global insolvency haircut fires, hand-proving the old formula overshoots by exactly 5 (extracting 45 against a 40 netFusd) while the fix lands within floor-division dust of exactly 40 |
| `TokenLogic._updateCooldownTimestamp()` floors its weighted-average mint timestamp division (`(prevTs*prevPrincipal + now*amount) / newPrincipal`). Because every later mint is weighted against the already-rounded timestamp, splitting one large mint into many small enough pieces makes each piece's own time contribution round away to nothing — `cooldownPrincipal` keeps growing while `cooldownTimestamp` stands still. This lets a large, already cooldown-matured principal "launder" a fresh, still-cooling mint through many tiny calls: at the finding's own worked example, ~22,459 calls of ≥5 fUSD added 240,000 fUSD to an 800,000 fUSD principal without moving the timestamp at all, where one equivalent 240,000 fUSD mint would have advanced it by ~19,754 seconds and stayed locked (FNA-55) | The division now rounds UP (`Math.ceilDiv`) instead of down. This is provably sufficient on its own: by induction, a rounded-up weighted timestamp can only ever overestimate the true (unrounded) weighted average at every step, so the numerator carried into each subsequent split is always ≥ the true cumulative time-weighted total — meaning the final split-computed timestamp is always ≥ the true weighted average, which is itself ≥ what a single equivalent mint's own one-step `ceilDiv` produces. No sequence of splits, adversarial or not, can therefore land an earlier cooldown expiry than one equivalent mint; if anything, repeated rounding-up costs the splitting party *more* delay, never less. New `TestTokenLogicCooldown` harness exposes `_updateCooldownTimestamp()` directly (plus a batched multi-step entry point) so an adversarial split sequence and its single-mint equivalent can be driven within the same block/timestamp without needing one real transaction per split — regression tests reproduce both the finding's own adversarial split shape and an arbitrary uneven split, in both cases asserting the split's resulting timestamp is never earlier than the single mint's |
| `AssetHandler._checkSequencerUp()` returns silently when `sequencerUptimeFeed` is unset, so the sequencer-down/grace-period protection is inert unless a feed address is set post-deployment. `initialize()` never configures it, `setSequencerUptimeFeed()` is `onlyOwner` with no other call site, and neither `deploy_core_contracts.ts` nor `upgrade_core_contracts.ts` called it — confirmed via direct `eth_call` (2026-08-30) that the live AssetHandler proxy's `sequencerUptimeFeed()` is still the zero address, despite `docs/security.md` stating in three places that the control is active and `docs/deployments.md` recording the canonical Base feed address (FNA-50) | `deploy_core_contracts.ts` now sets `SEQUENCER_UPTIME_FEED` (Chainlink's canonical Base L2 Sequencer Uptime Feed) unconditionally — not product-gated like `EUR_USD_FEED`, since Base is the only target chain regardless of `PRODUCT` — and verifies it actually landed before ownership transfers to `GOVERNANCE_SAFE`, throwing (failing the whole deploy) if it didn't. `upgrade_core_contracts.ts` now includes a third `GOVERNANCE_SAFE`-signed transaction (added to the existing `eoaBatch`, alongside the two ProxyAdmin upgrades) to set it on the already-live proxy. This is issued as a plain call directly on the AssetHandler proxy rather than bundled into `upgradeAndCall`'s post-upgrade `data` parameter: for a transparent proxy, that data executes via a delegatecall relayed through the fallback, so `msg.sender` as seen by the callee would be the ProxyAdmin *contract's* address, not `GOVERNANCE_SAFE` — and `AssetHandler.owner()` is confirmed on-chain to be `GOVERNANCE_SAFE` itself, not its ProxyAdmin, so a call routed that way would revert `onlyOwner`. A plain transaction directly against the proxy avoids that distinction entirely, and doesn't need atomicity with the implementation upgrade anyway, since `setSequencerUptimeFeed()` already exists on the currently-live implementation. The three `docs/security.md` mentions now carry an inline note pointing at this gap and the pending remediation transaction until it's actually executed on mainnet |
| `MorphoVaultV2Manager.setPoolVaults()`/`AaveV4TokenizationManager.setPoolVaults()` revoke every vault omitted from the replacement list, and both contract guards checked `isValidPoolVault` unconditionally in `txGuard()`, before classifying the selector — so delisting a vault before its position was fully unwound blocked manager-directed `withdraw()`/`redeem()` through the guarded `execTransaction()` path too, contradicting both managers' own documented intent ("revoking a vault here can never trap a pool's existing position"). The automatic pro-rata `withdrawProcessing()` paths were already unaffected (they never consult either allowlist), so the risk was scoped to the manual path and to queued withdrawals that depend on it to bring value back into `PoolLogic`'s own settlement balance (FNA-51) | Mirrors the `poolReserves`/`trackedPoolReserves` split `AaveV4SpokeManager` already uses for the identical class of bug (FNA-10): both vault managers gained a `trackedPoolVaults`/`isTrackedPoolVault` superset that `setPoolVaults()` never shrinks (only a new, permissionless `pruneTrackedVault()` does, and only once the vault is both delisted and holds exactly zero pool shares). Both contract guards now check the active allowlist (`isValidPoolVault`) only for entry-side operations (deposit/mint) and the tracked set (`isTrackedPoolVault`) for exit-side operations (withdraw/redeem, and — for Morpho Vault V2 — `forceDeallocate`/`multicall`, since FNA-46 already made those exit-only in spirit); `isSupportedAsset` stays a common, unconditional check for every operation either way. Resolves all three of the finding's own questions: full unwind before delisting is no longer required (exit stays available regardless), manager-directed withdraw/redeem now match the managers' documented exit guarantee, and a stalled queued withdrawal recovers by calling `withdraw()`/`redeem()` on the still-tracked vault through the normal manual path — re-listing or supplying extra liquidity are no longer the only options |
| `MorphoBlueManager.setPoolMarkets()` shares FNA-51's exact bug for Morpho Blue markets, but with a wider blast radius: `MorphoCollectLib`'s `getBalance`/`getDeficit`/`collectDebts`/`collectSupplies`/`collectCollaterals` and `MorphoChecksLib`'s `removeAssetCheck`/`removeTokenCheck` all enumerated `getPoolMarkets()` (the active allowlist) directly — unlike the Vault V2/Tokenization/Aave V4 Spoke integrations, where valuation/removal-safety never depended on the allowlist at all. Delisting a market with an open position therefore didn't just block manager-directed `withdraw()`/`repay()`/`withdrawCollateral()` via `execTransaction()` (mirroring FNA-51) — it made that market's debt/collateral/supply invisible to NAV, to debt-repayment planning, and (most dangerously) to the pool-level asset-removal safety check, letting `changeAssets()` remove a loan token from `supportedAssets` while real, unrecognized Morpho debt remained open in the now-invisible market. Separately, `PoolManagerLogic._removeAsset()`'s own guard resolution compounded this: if `AssetHandler.removeAsset()` ran before the pool-level removal (zeroing `assetTypes(_asset)`, per the finding's own described sequence), `getAssetGuard()` returned `address(0)` and the `removeAssetCheck()` call was skipped entirely — not given a wrong answer, never even invoked (FNA-52) | Mirrors `AaveV4SpokeManager`'s `trackedPoolReserves` split (FNA-10) again: `MorphoBlueManager` gained `trackedPoolMarkets`/`isTrackedPoolMarket`, a superset `setPoolMarkets()` never shrinks, plus a permissionless `pruneTrackedMarket()` gated on (tracked, not active, zero position). `MorphoCollectLib` and `MorphoChecksLib` now enumerate `getTrackedPoolMarkets()` instead of `getPoolMarkets()`, so a delisted-but-open market stays fully valued, stays in debt/withdrawal planning, and still blocks pool-level removal. `MorphoBlueContractGuard` splits its allowlist check by direction: entry (`supply`/`borrow`/`supplyCollateral`/`liquidate`) stays gated on the active allowlist; exit (`withdraw`/`repay`/`withdrawCollateral`) now checks the tracked set instead. Independently, `PoolManagerLogic._removeAsset()` now reverts (`NoAssetGuard`) rather than silently skipping `removeAssetCheck()` whenever the guard can't be resolved at all — closing the general form of the finding's sequencing gap for any asset type, not just Morpho, with the same recovery path the finding itself recommends: restore the asset's type/guard registration, then retry. **CertiK's 08/31 follow-up**: `pruneTrackedMarket()` is deliberately permissionless (see its own docs — the on-chain zero-position check is meant to be the real gate, not caller identity), but that check read the pool's position from a caller-supplied `morpho` address parameter, not a trusted one — anyone could pass a stub contract whose `position()` always returns an empty `Position`, satisfy the "zero live position" condition against that fake, and untrack a market that still held a live position on the *real* Morpho, restoring the exact NAV/withdrawal-safety gap this finding closed. Fixed exactly per the recommendation: `morpho` is now a constructor-fixed `immutable` on `MorphoBlueManager`, not a per-call parameter — `pruneTrackedMarket(pool, market)` dropped the argument entirely, so there is no longer any address for a caller to spoof. **Live-mainnet note**: `MorphoBlueManager` is already deployed on Base (`0x7C700a8...`, see `docs/deployments.md`) as a plain, non-upgradeable `Ownable` contract — this fix requires deploying a *new* instance (constructor now takes the real Morpho Blue core address) and re-registering every existing pool's markets on it via `setPoolMarkets()`, then re-pointing every consumer that took the old manager's address as its own immutable constructor argument (`MorphoBlueContractGuard`, `MorphoBlueLendingPoolAssetGuard`) at the new one — the code fix alone does not protect the currently-live manager instance until that migration happens |
| `removeTokenCheck()` — the cross-asset dependency check already implemented by seven guards (`ERC20Guard` and every guard built on `ClosedAssetGuard`: Aave V3/V4 lending, Morpho Blue, Morpho Vault V2, Aave V4 Tokenization/Spoke, Uniswap V3) — was only ever actually invoked from `ERC20Guard.removeAssetCheck()`'s own loop, so it only ran when the asset being *removed* happened to be ERC20Guard-typed. Every composite/position guard inherits `ClosedAssetGuard` instead, whose `removeAssetCheck()` only checks its own pool balance and never asks any other guard whether it still depends on the asset being removed. A composite ERC-20 (e.g. a Morpho Vault V2 share) used as a Uniswap V3 position leg could therefore be removed from `supportedAssets` while an open Uniswap V3 NFT still referenced it as `token0`/`token1` — `UniswapV3AssetGuard` then silently skips that leg in `getBalance()` without reporting the reading as incomplete, understating NAV until the misconfiguration is corrected (FNA-53) | Centralized: the cross-guard loop moved out of `ERC20Guard.removeAssetCheck()` into a new `PoolManagerLogic._requireNotReferencedByOtherAssets()`, called from `_removeAsset()` for every removal regardless of the candidate's own guard type — reusing the `removeTokenCheck()` mechanism seven guards already implement instead of adding a new one. `ERC20Guard.removeAssetCheck()` no longer duplicates the loop, keeping only its own balance check. A different supported asset whose own guard can't be resolved is skipped (not blocking removal), matching this loop's pre-existing fail-open-for-others convention — only the *candidate* asset's own guard resolution is fail-closed (FNA-52) |
| `PoolManagerLogic.getAssetPrice()` returned AssetHandler's registered feed verbatim for every asset, including a pre-valued asset's placeholder $1.00 identity aggregator (the sentinel that lets `getBalance()`'s already-priced USD figure survive `assetValue()`'s multiplication unchanged) — nothing distinguished that sentinel from a genuine unit price. Five raw-amount consumers were named: `CLPriceLibrary.getFairSqrtPriceX96()` (builds a Uniswap V3 fair-price band from it), `AaveV4TokenizationAssetGuard`/`MorphoVaultV2AssetGuard` (value a redeemed underlying amount, and separately use the same call as a zero-price admission check that a nonzero sentinel wrongly passes), `AaveV4SpokeAssetGuard` (per-reserve valuation feeding `getBalance()`), and `UniswapV3AssetGuard` (values each pool leg behind only an `isSupportedAsset` check a pre-valued asset satisfies) (FNA-56) | Already substantially closed by the FNA-45 08/31 follow-up fix (same root cause, identical recommended fix — `IPreValuedAssetGuard.getUnitPrice()`, dispatched from `PoolManagerLogic.getAssetPrice()`), which predates this finding's own filing. Verified all five named consumers by tracing their call chains rather than assuming: `CLPriceLibrary`/`UniswapV3AssetGuard` both resolve `factory` via `IPoolLogic(pool).factory()` → `PoolManagerLogic.factory()` → `address(this)` (the same convention every other guard in this codebase already relies on), so both route through the now-fixed dispatch with no separate code change needed; `AaveV4TokenizationAssetGuard`/`MorphoVaultV2AssetGuard`'s `addAssetCheck()`/`_valueShares()` price their vault's real `underlying` (not the share itself) via `IPoolManagerLogic.getAssetPrice()`, likewise already fixed at the source; `AaveV4SpokeAssetGuard` prices each reserve's `underlying` the same way. This session's own residual work for this finding specifically: (1) adopted `Math.mulDiv` (512-bit intermediate) in both `getUnitPrice()` implementations per this finding's own suggested code, replacing plain `*`/`/` to remove a theoretical intermediate-overflow path for a very large balance priced through a high-value underlying; (2) extracted a shared `PoolManagerLogic._isPreValued(guard)` internal helper — the same low-level-staticcall marker check was duplicated three times in that file (`getAssetPrice()`, `assetValue()`, and `_addAsset()`'s FNA-18 deposit check) — reducing duplication and, as a side effect, net bytecode (~400 bytes) rather than costing any; (3) added the specific regression tests this finding's own checklist named that FNA-45's tests didn't yet cover: a share token that is itself 6-decimal (not just a 6-decimal *underlying*, which was already covered), and two-level "nesting" (a pre-valued asset whose own underlying is itself a different pre-valued asset) proving the cascade returns the innermost real price and correctly propagates an innermost revert, exercised through the real `PoolManagerLogic` dispatch (not a mock that fakes the cascade) via a new `MockAssetGuard.setForwardPriceToAsset()` test helper. "EUR accounting" from the same checklist needed no dedicated test: `getUnitPrice()` has no currency-specific branching of its own — it prices through whatever `getAssetPrice()`/`AssetHandler.getUSDPrice()` already returns for the underlying, so EUR-mode correctness is inherited automatically, and `contracts/contracts/` stays byte-identical between the USD and EUR product branches |
| For a pool with tracked Morpho debt in at least two different loan tokens, `MorphoBlueLendingPoolAssetGuard._swapSettlementToDebts()` buys the non-settlement debt leg via an exact-output Uniswap V3 swap sized to `_bufferedRepay(toAssetsUp(repayBorrowShares, ...))` — the exact repay amount inflated by `repayDebtBufferBps` (0.20% default) — but `_repayDebts()` then repays Morpho by the exact, unbuffered `repayBorrowShares` via `repay(..., shares: repayBorrowShares, ...)`, so Morpho only ever pulls the unbuffered amount. The purchased surplus isn't collected anywhere: `_withdrawAllAssets()` only gathers supply/collateral legs, never a leftover debt-token balance, so it sits as an idle, oracle-valued pool balance benefiting remaining holders while the withdrawing holder is paid only the settlement-token balance increase — under-paid by approximately `repayDebtBufferBps` × the cross-token debt leg. Requires >= 2 distinct Morpho loan tokens tracked by the same pool to trigger at all (the checked deployment's markets are USDC-only, so this was inactive at review time) (FNA-57) | `_swapSettlementToDebts()` no longer buffers the exact-output `amountOut` — it now uses `toAssetsUp(repayBorrowShares, ...)` directly, exactly matching what `_repayDebts()`'s share-based `repay()` call actually consumes, so the swap can never buy more debt token than gets spent. Per the finding's own recommendation, safety margin stays exactly where it already was rather than being removed outright: `MorphoMathLib.oracleMaxIn()`'s own `slippageBps` parameter still pads `amountInMaximum` (the settlement-token spend side) for the swap's own price movement, and `_estimateFlashAmount()`'s separate, untouched `_bufferedRepay()`/`flashAmountBufferBps` calls still over-size the flashloan borrowed up front — neither of those needed to change for the exact-output purchase to keep real execution headroom. `_approveMorphoForAllDebts()`'s own buffered approval ceiling was also left untouched, since an approval limit costs nothing on its own and only bounds what Morpho's exact-share repay is allowed to pull, never what it actually does. New regression tests hand-derive the exact ceiling-rounded repay amount for a cross-token leg (proving the fix's `250,001` against the pre-fix formula's `250,501`, verified against both the fixed and — by temporarily reverting — the original code) and confirm a same-token (no-swap) debt leg is unaffected |

---

## Audit Scope

The primary audit-relevant contracts are:

- `contracts/contracts/TokenLogic.sol`
- `contracts/contracts/PoolLogic.sol`
- `contracts/contracts/PoolManagerLogic.sol`
- `contracts/contracts/Governance.sol`
- `contracts/contracts/guards/` (all guards)
- `contracts/contracts/priceAggregators/AssetHandler.sol`
- `contracts/contracts/utils/` (library code)
